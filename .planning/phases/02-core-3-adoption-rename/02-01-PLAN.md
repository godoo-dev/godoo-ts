---
phase: 02-core-3-adoption-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/package.json
  - packages/client/tsconfig.json
  - packages/client/tsdown.config.ts
  - packages/client/src/**
  - packages/client/tests/**
  - packages/client/README.md
  - packages/client/LICENSE
  - tsconfig.json
  - vitest.config.ts
  - pnpm-lock.yaml
  - packages/_example/**
autonomous: true
requirements:
  - CORE-01
  - CORE-05
user_setup: []
tags:
  - typescript
  - pnpm
  - tsdown
  - vitest
  - monorepo
  - adoption

must_haves:
  truths:
    - "T1: Three @godoo/* packages live under packages/ at phase end and packages/_example is gone. (this plan establishes 1/3 — @godoo/client — and deletes _example)"
    - "T2: Every package.json .name is @godoo/<pkg>; zero @marcfargas/odoo-* strings remain anywhere in the workspace at phase end. (this plan: zero @marcfargas/odoo-client references in packages/client/ after commit)"
    - "T3: tsdown ESM-only output — exports['.'].import ends in .mjs; .exports['.'].types ends in .d.mts; no .js/.d.ts in any exports map."
    - "T4: Per-package tsconfig.json extends ../../tsconfig.base.json and sets composite: true; root tsconfig.json references[] lists the package."
    - "T6: pnpm biome check ., pnpm tsc --noEmit, pnpm build, pnpm test (unit) all exit 0 across whole workspace after the adoption commit."
    - "T7: 10 client integration tests are marked describe.skip with inline // TODO(CORE-03) comments; root vitest config excludes *.integration.test.ts so unit `pnpm test` is clean."
    - "T10: Adoption commit subject matches: feat(client): adopt @marcfargas/odoo-client as @godoo/client (from odoo-toolbox@<short-sha>)."
    - "Pre-adoption-baseline tag exists on odoo-toolbox HEAD before any adoption commit lands."
    - "D-01: pre-adoption-baseline tag created in Task 0; plain-copy via whitelist (Task 1); adoption commit subject references odoo-toolbox source SHA (Task 5); no git subtree."
    - "D-02: jest devDeps (jest, ts-jest, @types/jest) dropped and scripts.test rewritten to 'vitest run' in the same adoption commit as the @godoo/client rename (Task 2)."
    - "D-03: strict-TS conformance — Domain/RpcArg introduced, ~28 'any' occurrences eliminated, zero @ts-ignore/@ts-nocheck/biome-ignore; workspace gates 'pnpm biome check . && pnpm tsc --noEmit && pnpm build' exit 0 after the commit (Tasks 2 + 4)."
    - "D-06: locked adoption order honored — @godoo/client is adopted first (this plan, wave 1); integration tests deferred behind describe.skip + TODO(CORE-03) markers until 02-03 re-enables them."
    - "D-07: packages/_example is deleted in the same commit as the @godoo/client adoption (Task 3 deletes the directory; Task 5 commits it)."
  artifacts:
    - path: "packages/client/package.json"
      provides: "Renamed @godoo/client manifest with tsdown ESM exports + vitest run script + workspace metadata"
      contains: '"name": "@godoo/client"'
    - path: "packages/client/tsconfig.json"
      provides: "Per-package strict TS config extending workspace base"
      contains: '"extends": "../../tsconfig.base.json"'
    - path: "packages/client/tsdown.config.ts"
      provides: "ESM-only tsdown build config"
      contains: "format: 'esm'"
    - path: "packages/client/src/index.ts"
      provides: "Public API surface re-exporting client/services/rpc/types/safety plus Domain/RpcArg types"
      exports: ["OdooClient", "createClient", "Domain", "RpcArg"]
    - path: "tsconfig.json"
      provides: "Root project-references entry for @godoo/client"
      contains: "packages/client"
    - path: "vitest.config.ts"
      provides: "Root vitest config excluding *.integration.test.ts from `pnpm test`"
      contains: "*.integration.test.ts"
  key_links:
    - from: "packages/client/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends"
      pattern: 'extends.*tsconfig\.base\.json'
    - from: "tsconfig.json"
      to: "packages/client/tsconfig.json"
      via: "references[]"
      pattern: 'packages/client'
    - from: "packages/client/package.json"
      to: "packages/client/dist/index.mjs"
      via: "exports.import"
      pattern: '\.mjs'
---

<objective>
**Phase Goal (user story — repeated in every Phase-2 plan for context):**

**As a** TypeScript developer adopting the godoo core libraries, **I want to** install `@godoo/client`, `@godoo/testcontainers`, and `@godoo/introspection` from the public `@godoo/` scope (with no `@marcfargas/odoo-*` strings left in the source) **so that** I can manage real Odoo ERP instances against a single canonical TS home that builds, typechecks, lints, and tests green on Node 22 + 24.

**This plan (02-01):** Tag `odoo-toolbox` at `pre-adoption-baseline`, plain-copy `odoo-client` into `packages/client/`, rename to `@godoo/client`, complete the Phase-1 toolchain conversion (tsdown ESM-only, per-package tsconfig with `composite: true`, exports map, dead-devDep cleanup), apply the D-03 strict-TS pass (define `Domain` / `RpcArg`, eliminate ~28 `any` lines), skip 10 integration test files behind `describe.skip` + `// TODO(CORE-03)`, add a root vitest `exclude` for `*.integration.test.ts`, delete `packages/_example/` (D-07), update root `tsconfig.json` `references[]`, commit lockfile alongside in one `feat(client): ...` commit referencing the source SHA.

Purpose: Lands the foundational `@godoo/client` package — every other adoption (02-02, 02-04) imports from it via `workspace:*`. Defines the canonical `Domain` / `RpcArg` types reused in 02-02 and 02-04. Replaces the throwaway `_example` with a real package.

Output: One adoption commit on `develop` containing the new `packages/client/` tree, deleted `packages/_example/`, updated root tsconfig + vitest config, updated lockfile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-3-adoption-rename/02-CONTEXT.md
@.planning/phases/02-core-3-adoption-rename/02-RESEARCH.md
@.planning/phases/02-core-3-adoption-rename/02-PATTERNS.md
@.planning/phases/01-repo-toolchain-bootstrap/01-VERIFICATION.md
@CLAUDE.md
@../godoo-hq/.planning/notes/godoo-adoption-protocol.md

<interfaces>
New types to introduce in `packages/client/src/rpc/types.ts` and re-export from `packages/client/src/index.ts` (per RESEARCH.md Open Question #2 pragmatic shape — to be imported by 02-02 and 02-04):

  export type DomainClause = [string, string, unknown] | '&' | '|' | '!';
  export type Domain = DomainClause[];
  export type RpcArg = unknown;

Existing top-level exports preserved from source (re-export * from './client' | './services' | './rpc' | './types' | './safety'): OdooClient, OAuthProxyClient, ModuleManager, createClient, OdooAuthError, plus service-specific functions (mail/accounting/attendance/timesheets/properties/urls/cdc).
</interfaces>
</context>

<source_audit>
## Multi-Source Coverage Audit (Phase 02 — recorded once in Plan 02-01)

### GOAL coverage
- Three core libraries adopted: 02-01 (client), 02-02 (testcontainers), 02-04 (introspection)
- Renamed under @godoo/: same plans above
- Fully validated incl. client integration tests vs. real Odoo: 02-03

### REQ coverage (every ID MUST appear in a plan)
- CORE-01 → 02-01
- CORE-02 → 02-02
- CORE-03 → 02-03
- CORE-04 → 02-04
- CORE-05 → 02-01, 02-02, 02-04 (each removes `@marcfargas/odoo-*` per-package; verified end-to-end after 02-04)

### RESEARCH coverage
- Source package shapes → 02-01 / 02-02 / 02-04
- jest→vitest cleanup → 02-01, 02-04 (zero API rewrites per RESEARCH §Jest→Vitest Conversion)
- Strict-TS Domain/RpcArg → 02-01 defines, 02-02/02-04 re-import
- Import-path rewrite scope → 02-01 (3 JSDoc), 02-02 (1 real + 3 JSDoc), 02-04 (4 real + 1 test + JSDoc)
- Integration-test reactivation (globalSetup, vitest.integration.config.ts) → 02-03
- Integration CI workflow + branch ruleset update → 02-03
- Toolchain conversion delta (exports map, tsconfig, tsdown.config.ts) → per-package each plan
- Container snapshot caching → 02-03 (default-on; verified)
- Bin shebang for introspection CLI → 02-04
- pre-adoption-baseline tag → 02-01 Task 0
- Adoption commit subject convention → every plan

### CONTEXT coverage (D-01..D-07 + Claude's Discretion)
- D-01 fresh copy + tag → 02-01 (tag + first copy), 02-02 (copy), 02-04 (copy)
- D-02 jest→vitest one-pass → 02-01 (client), 02-04 (introspection); 02-02 already vitest
- D-03 strict-clean → every plan
- D-04 integration CI job → 02-03
- D-05 container orchestration via @godoo/testcontainers → 02-03
- D-06 locked order client→testcontainers→reenable→introspection → wave structure 02-01→02-02→02-03→02-04
- D-07 delete _example in first adoption commit → 02-01
- Biome reformat folded into D-03 → every plan
- debug dep stays per-package → every plan
- workspace:* cross-package deps → 02-02, 02-04
- CHANGELOG.md baseline deferred to Phase 3 → every plan deletes inherited CHANGELOG.md
- pre-adoption-baseline tag only (no branch creation) → 02-01
- package.json metadata refresh → every plan
- vitest config layout (per-package for testcontainers + client-integration; root for client-unit/introspection) → 02-02, 02-03

### Gaps
None. Every GOAL slice, every CORE-* requirement, every RESEARCH item, every CONTEXT decision maps to a plan.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 0: Tag source repo at pre-adoption-baseline + capture source SHA</name>
  <files>
    (no godoo-ts files modified; affects sibling C:\dev\odoo-toolbox only)
  </files>
  <read_first>
    - ../godoo-hq/.planning/notes/godoo-adoption-protocol.md (canonical step 1: source-side tag pre-adoption-baseline)
    - .planning/phases/02-core-3-adoption-rename/02-RESEARCH.md §"Pitfall 6: Phase-2 lands but `git -C odoo-toolbox tag pre-adoption-baseline` was never run"
    - .planning/phases/02-core-3-adoption-rename/02-CONTEXT.md D-01
  </read_first>
  <action>
    Tag HEAD of `C:\dev\odoo-toolbox` as `pre-adoption-baseline` if the tag does not already exist (no `-f`; idempotent). Capture short SHA of HEAD via `git -C C:\dev\odoo-toolbox rev-parse --short=10 HEAD` for use in Task 5 commit subject as `(from odoo-toolbox@<SHA>)` per D-01. Do NOT push the tag (local-only; pins Phase-3 source-side shed baseline). Do NOT create the `godoo-adoption` branch on the source repo (Phase 3 per CONTEXT.md Claude's Discretion item). Source repo's working tree must remain clean.
  </action>
  <verify>
    Run `git -C C:\dev\odoo-toolbox tag -l pre-adoption-baseline` — must print exactly `pre-adoption-baseline`. Run `git -C C:\dev\odoo-toolbox status --porcelain` — must be empty.
  </verify>
  <acceptance_criteria>
    - `git -C C:\dev\odoo-toolbox tag -l pre-adoption-baseline` outputs `pre-adoption-baseline`
    - `git -C C:\dev\odoo-toolbox rev-parse pre-adoption-baseline^{commit}` exits 0
    - `git -C C:\dev\odoo-toolbox rev-parse --short=10 HEAD` returns a 10-char hex SHA
    - `git -C C:\dev\odoo-toolbox status --porcelain` outputs nothing (zero working-tree mutation)
  </acceptance_criteria>
  <done>
    `pre-adoption-baseline` tag exists locally on odoo-toolbox HEAD; SOURCE_SHA captured for the Task 5 commit subject; source working tree untouched.
  </done>
</task>

<task type="auto">
  <name>Task 1: Whitelist-copy odoo-client source into packages/client/</name>
  <files>
    packages/client/src/** (47 .ts files mirrored from source)
    packages/client/tests/** (22 .ts files mirrored: 12 *.test.ts unit + 10 *.integration.test.ts)
    packages/client/README.md
    packages/client/LICENSE
  </files>
  <read_first>
    - .planning/phases/02-core-3-adoption-rename/02-RESEARCH.md §"Source Package Shapes — @marcfargas/odoo-client → @godoo/client"
    - .planning/phases/02-core-3-adoption-rename/02-RESEARCH.md §"Pitfall 1: Copying dist/ or tsconfig.tsbuildinfo from source"
    - .planning/phases/02-core-3-adoption-rename/02-PATTERNS.md Pattern G (Import-path rewrite scope for client)
    - C:\dev\odoo-toolbox\packages\odoo-client\ (source tree — read-only)
  </read_first>
  <action>
    Plain-copy from `C:\dev\odoo-toolbox\packages\odoo-client\` into `C:\dev\godoo-dev\godoo-ts\packages\client\` using an explicit whitelist: `src/`, `tests/`, `README.md`, `LICENSE`. EXCLUDE every other source-side entry — specifically: `dist/`, `node_modules/`, `tsconfig.tsbuildinfo`, `*.tgz`, `*.log`, `examples/`, `CHANGELOG.md` (Phase 3 regenerates via changesets; carrying it forward leaves stale `@marcfargas/odoo-client` strings that would fail CORE-05's `git grep` check), source `package.json` (rewritten fresh in Task 2), source `tsconfig.json` (rewritten fresh in Task 2), `jest.config.*` (none expected per RESEARCH §Jest→Vitest, but delete on sight if found). Preserve file mode/permissions.
  </action>
  <verify>
    File count + structure check: count `.ts` files under `packages/client/src/` (expect ≥40) and under `packages/client/tests/` (expect ≥20). Verify `packages/client/dist/`, `packages/client/node_modules/`, `packages/client/CHANGELOG.md`, `packages/client/tsconfig.tsbuildinfo` do NOT exist.
  </verify>
  <acceptance_criteria>
    - `packages/client/src/` contains 47 `.ts` files organized into `client/`, `rpc/`, `safety/`, `services/{accounting,attendance,cdc,mail,modules,properties,timesheets,urls}/`, `types/` (per RESEARCH.md inventory)
    - `packages/client/tests/` contains 22 `.ts` files (12 `*.test.ts` + 10 `*.integration.test.ts`)
    - `packages/client/README.md` exists
    - `packages/client/LICENSE` exists
    - `packages/client/dist/` does NOT exist
    - `packages/client/node_modules/` does NOT exist
    - `packages/client/CHANGELOG.md` does NOT exist (excluded for CORE-05 cleanliness)
    - `packages/client/tsconfig.tsbuildinfo` does NOT exist
    - `packages/client/jest.config.*` does NOT exist
  </acceptance_criteria>
  <done>
    Source files mirrored via whitelist; zero build artifacts, lockfiles, or inherited CHANGELOG carried over.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write package.json + tsconfig.json + tsdown.config.ts + rename imports + .js extensions + Domain/RpcArg types + strict-TS pass + skip integration tests</name>
  <files>
    packages/client/package.json (NEW — written fresh from PATTERNS.md skeleton)
    packages/client/tsconfig.json (NEW — written fresh from _example shape)
    packages/client/tsdown.config.ts (NEW — written fresh from _example shape)
    packages/client/src/index.ts (modify — re-export Domain / RpcArg / DomainClause)
    packages/client/src/rpc/types.ts (modify — define DomainClause / Domain / RpcArg; replace any[] usage)
    packages/client/src/client/{config.ts, oauth-proxy-client.ts, odoo-client.ts, types.ts} (modify — JSDoc @example rewrites + any → typed)
    packages/client/src/rpc/{bearer-transport.ts, transport.ts} (modify — any → typed)
    packages/client/src/services/{index.ts, accounting/accounting-service.ts, accounting/functions.ts, attendance/**, modules/**, timesheets/**} (modify — JSDoc @example rewrites + any → typed)
    packages/client/tests/**/*.test.ts (modify — add `.js` extension to all `../src/...` imports under nodenext)
    packages/client/tests/**/*.integration.test.ts (modify — `.js` extension + describe.skip wrapper + TODO(CORE-03) comment)
    packages/client/README.md (modify — 5 occurrences of @marcfargas/odoo-client → @godoo/client; install snippet → `pnpm add @godoo/client`)
  </files>
  <behavior>
    - After this task: `pnpm install` (Task 4) resolves the new workspace package `@godoo/client`; `pnpm tsc --noEmit` over the whole workspace exits 0.
    - `Domain` / `RpcArg` / `DomainClause` are exported from `@godoo/client` package root for 02-02 / 02-04 to import.
    - `pnpm test` from workspace root (after Task 3 vitest exclude) runs only `*.test.ts` files, not `*.integration.test.ts`; all client unit tests pass.
    - `pnpm --filter @godoo/client run test` reports integration suites as `skipped`, not `failed`.
    - `git grep "@marcfargas/odoo-client" packages/client/` returns zero matches.
    - `pnpm biome check packages/client` exits 0 — zero `noExplicitAny` violations, zero `noUnusedVariables`, zero `// biome-ignore` suppressions.
  </behavior>
  <action>
    Write `packages/client/package.json` per RESEARCH.md §Toolchain Conversion Delta + PATTERNS.md §packages/client/package.json. Concrete values:
    - `.name` = `"@godoo/client"`
    - `.version` = `"0.6.0"` (inherit; Phase 3 rebaselines)
    - `.type` = `"module"`
    - `.license` = `"LGPL-3.0"`
    - `.description` inherits from source (rewrite any `@marcfargas/odoo-client` to `@godoo/client`)
    - `.main` = `"./dist/index.mjs"`
    - `.types` = `"./dist/index.d.mts"`
    - `.exports["."]` = `{ "import": "./dist/index.mjs", "types": "./dist/index.d.mts" }` — CRITICAL: `.mjs`/`.d.mts`, not `.js`/`.d.ts` (Phase-1 landmine #1)
    - `.files` = `["dist", "README.md", "LICENSE"]`
    - `.scripts.build` = `"tsdown"`
    - `.scripts.test` = `"vitest run"` (per D-02, replacing the stale `"jest"` script lie)
    - `.scripts.dev` = `"tsdown --watch"` (optional)
    - `.dependencies` = `{ "debug": "^4.4.3" }`
    - `.devDependencies` = `{ "@types/debug": "^4.1.12" }` — DROP `jest`, `ts-jest`, `@types/jest`, `typescript`, `vitest` (workspace root provides; D-02 dead-devDep cleanup)
    - `.repository` = `{ "type": "git", "url": "https://github.com/godoo-dev/godoo-ts.git", "directory": "packages/client" }`
    - `.homepage` = `"https://github.com/godoo-dev/godoo-ts/tree/main/packages/client"`
    - `.author` inherits from source
    - `.keywords` inherits from source (verify no `@marcfargas/odoo` strings remain)
    - NO `.private` key (this package will be published in Phase 3)
    - NO `.peerDependencies` (client is the root of the dep graph)

    Write `packages/client/tsconfig.json` per PATTERNS.md Pattern B (verbatim from `packages/_example/tsconfig.json`):
    - `.extends` = `"../../tsconfig.base.json"`
    - `.compilerOptions.composite` = `true`
    - `.compilerOptions.rootDir` = `"./src"`
    - `.compilerOptions.outDir` = `"./dist"`
    - `.include` = `["src", "tests"]`
    - NO `exclude` key (source's `exclude` would skip tests from typecheck; drop it — Phase-1 _example pattern includes tests in typecheck under strict + isolatedDeclarations).

    Write `packages/client/tsdown.config.ts` per PATTERNS.md Pattern C:
    - Imports `defineConfig` from `'tsdown'`
    - Exports default `defineConfig({ entry: ['./src/index.ts'], format: 'esm', dts: true, clean: true, platform: 'node' })`

    Define new types in `packages/client/src/rpc/types.ts` (alongside existing RPC types). Identifiers and shape per RESEARCH.md Open Question #2 pragmatic recommendation:
    - `export type DomainClause = [string, string, unknown] | '&' | '|' | '!';`
    - `export type Domain = DomainClause[];`
    - `export type RpcArg = unknown;`
    Re-export them from `packages/client/src/index.ts` (alongside the existing `* from './client' | './services' | './rpc' | './types' | './safety'`).

    Strict-TS sweep (D-03 + RESEARCH §Strict-TS Gap Audit): Replace every `any[]` and `any` in source code (~28 occurrences across 14 files enumerated in RESEARCH.md table — hotspots: `src/client/oauth-proxy-client.ts` (4), `src/client/odoo-client.ts` (4), `src/client/types.ts` (4), `src/rpc/bearer-transport.ts` (2), `src/rpc/transport.ts` (1), `src/rpc/types.ts` (1), `src/services/accounting/{accounting-service,functions}.ts` (5), `src/services/{attendance,modules,timesheets}/...` (3)). Use `Domain` for Odoo search-domain positions, `RpcArg[]` for positional RPC arguments, `unknown` + narrowing elsewhere. The `src/services/cdc/cdc-service.ts:1` `any` is in JSDoc — leave the JSDoc but ensure no real-code `any` remains. Per D-03: ZERO `// @ts-expect-error`, ZERO `// @ts-nocheck`, ZERO `// biome-ignore` suppressions may remain. Add explicit return types where `isolatedDeclarations: true` flags inferred returns — surface during `pnpm tsc --noEmit` run in Task 4 and iterate.

    Test-import `.js`-extension sweep (Phase-1 landmine #5; RESEARCH.md Pitfall #2): rewrite every extensionless `from '../src/...'` in every `packages/client/tests/**/*.test.ts` and `*.integration.test.ts` to add the `.js` suffix (e.g. `from '../src/client/odoo-client.js'`). Global edit across 22 test files; verify by `pnpm tsc --noEmit` (any miss surfaces as "Relative import paths need explicit file extensions"). Tests already import from `'vitest'` (no jest API anywhere per RESEARCH §Jest→Vitest), so no API rewrites are needed.

    Skip integration tests (D-06 step 1; RESEARCH §Plan 02-01 skip mechanism recommendation 1 + PATTERNS.md integration-test-skip pattern): in EACH of the 10 `packages/client/tests/**/*.integration.test.ts` files, wrap the top-level `describe(...)` with `describe.skip(...)` and prepend an inline comment EXACTLY: `// TODO(CORE-03): re-enable after @godoo/testcontainers lands (Phase 02-03)`. Plan 02-03 deletes both the `.skip` suffix and the TODO comment.

    JSDoc `@example` rewrite (RESEARCH §Import-Path Rewrite Scope client): rewrite 3 JSDoc-only `@marcfargas/odoo-client` references at `src/client/config.ts:50`, `src/client/oauth-proxy-client.ts:28`, `src/services/index.ts:11` to `@godoo/client`.

    README.md rewrite: replace all 5 `@marcfargas/odoo-client` occurrences with `@godoo/client`; update install snippets to `pnpm add @godoo/client`. Update any repo URLs to `godoo-dev/godoo-ts`.

    Reformat with Biome (Claude's Discretion: fold reformat into D-03 strict pass) — running `pnpm biome check . --write` over the new files is sufficient.

    NEVER place fenced code blocks in this action; concrete identifiers above are the contract.
  </action>
  <verify>
    Read `packages/client/package.json` and confirm `.name = "@godoo/client"`, `.exports["."].import = "./dist/index.mjs"`, `.exports["."].types = "./dist/index.d.mts"`. Run `git grep "@marcfargas/odoo-client" packages/client/` — must return zero matches. Spot-check `packages/client/src/rpc/types.ts` exports `Domain`, `DomainClause`, `RpcArg`. Spot-check 1 of the 10 `*.integration.test.ts` files contains `describe.skip(` and the exact TODO comment. Run `pnpm tsc --noEmit` from repo root (deferred to Task 4 for the full gate, but executor may run mid-task as a sanity check).
  </verify>
  <acceptance_criteria>
    - `jq -r '.name' packages/client/package.json` outputs `@godoo/client`
    - `jq -r '.exports["."].import' packages/client/package.json` outputs `./dist/index.mjs`
    - `jq -r '.exports["."].types' packages/client/package.json` outputs `./dist/index.d.mts`
    - `jq -r '.scripts.test' packages/client/package.json` outputs `vitest run`
    - `jq -r '.devDependencies.jest // "absent"' packages/client/package.json` outputs `absent`
    - `jq -r '.devDependencies["ts-jest"] // "absent"' packages/client/package.json` outputs `absent`
    - `jq -r '.devDependencies["@types/jest"] // "absent"' packages/client/package.json` outputs `absent`
    - `jq -r '.repository.directory' packages/client/package.json` outputs `packages/client`
    - `packages/client/tsconfig.json` contains the substring `"extends": "../../tsconfig.base.json"` and `"composite": true`
    - `packages/client/tsdown.config.ts` contains `format: 'esm'` and `dts: true`
    - `packages/client/src/index.ts` re-exports `Domain`, `DomainClause`, `RpcArg` (grep for those identifiers in the file)
    - `git grep -l "@marcfargas/odoo-client" packages/client/` returns zero files
    - `grep -c "describe.skip" packages/client/tests/*.integration.test.ts | awk -F: '{s+=$2} END {print s}'` equals 10
    - `grep -c "// TODO(CORE-03):" packages/client/tests/*.integration.test.ts | awk -F: '{s+=$2} END {print s}'` is at least 10
    - `git grep -nE '^\s*//\s*@ts-(ignore|expect-error|nocheck)' packages/client/src/` returns zero matches
    - `git grep -nE '//\s*biome-ignore' packages/client/src/` returns zero matches
    - Every `*.test.ts` or `*.integration.test.ts` import from `../src/...` ends in `.js` (verify by `grep -rnE "from '(\.\./)+src/[^'\"]+'" packages/client/tests/` — every matching line ends in `.js'`)
  </acceptance_criteria>
  <done>
    `packages/client/` is fully renamed to `@godoo/client`, toolchain-converted (tsdown ESM exports + per-package tsconfig + dropped jest devDeps), strict-TS-clean (Domain/RpcArg defined and applied, zero any/ts-ignore/biome-ignore), test imports use `.js` extensions, 10 integration tests are `describe.skip`-wrapped with TODO(CORE-03) markers. Ready for root tsconfig/vitest wiring (Task 3) and verification (Task 4).
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire root tsconfig references, root vitest exclude, delete packages/_example</name>
  <files>
    tsconfig.json (modify — replace `{ "path": "packages/_example" }` with `{ "path": "packages/client" }`)
    vitest.config.ts (modify — add `test.exclude: ['**/node_modules/**', '**/dist/**', '**/*.integration.test.ts']`)
    packages/_example/** (DELETE entire directory per D-07)
  </files>
  <read_first>
    - tsconfig.json (current 5-line file; PATTERNS.md §tsconfig.json shows current state)
    - vitest.config.ts (current 8-line file; PATTERNS.md §vitest.config.ts modification pattern)
    - .planning/phases/02-core-3-adoption-rename/02-CONTEXT.md D-07
    - .planning/phases/02-core-3-adoption-rename/02-RESEARCH.md §"Pitfall 4: Root vitest config picks up integration tests in pnpm test"
    - .planning/phases/02-core-3-adoption-rename/02-PATTERNS.md §tsconfig.json (root, modified — append references) + §vitest.config.ts (root, modified — exclude integration tests)
  </read_first>
  <action>
    (a) Edit root `tsconfig.json`: replace the existing `references[]` entry `{ "path": "packages/_example" }` with `{ "path": "packages/client" }`. Final state per PATTERNS.md:
        `{ "$schema": "https://json.schemastore.org/tsconfig", "files": [], "references": [{ "path": "packages/client" }] }`
    (b) Edit root `vitest.config.ts`: add `exclude` array to the `test` object so `pnpm test` skips integration tests. Vitest default `exclude` (node_modules, dist, cypress, .idea, etc.) is REPLACED — not extended — when `exclude` is set, so restate the node_modules + dist defaults. Final state:
        `import { defineConfig } from 'vitest/config';`
        `export default defineConfig({ test: { projects: ['packages/*'], exclude: ['**/node_modules/**', '**/dist/**', '**/*.integration.test.ts'] } });`
    (c) Delete the entire `packages/_example/` directory (D-07). This includes `packages/_example/{package.json, tsconfig.json, tsdown.config.ts, src/, tests/, README.md}` and any other files Phase 1 created. Verify by `test ! -d packages/_example`.
    Per CLAUDE.md scope-discipline: do NOT modify any other root config (biome.json, lefthook.yml, pnpm-workspace.yaml, package.json, tsconfig.base.json) in this plan — they remain at their Phase-1 state.
  </action>
  <verify>
    Confirm `tsconfig.json` references `packages/client` (not `_example`); confirm `vitest.config.ts` contains `**/*.integration.test.ts` in `exclude`; confirm `packages/_example/` is gone.
  </verify>
  <acceptance_criteria>
    - `jq -r '.references[].path' tsconfig.json` outputs exactly `packages/client` (single line, no `_example`)
    - `jq -e '.references | length == 1' tsconfig.json` exits 0
    - `grep -c "packages/_example" tsconfig.json` outputs `0`
    - `grep -F "**/*.integration.test.ts" vitest.config.ts` finds the substring (exit 0)
    - `grep -F "**/node_modules/**" vitest.config.ts` finds the substring (vitest defaults restated)
    - `test ! -d packages/_example` exits 0
    - `git status --porcelain packages/_example` shows deleted entries (not present in working tree)
  </acceptance_criteria>
  <done>
    Root tsconfig references @godoo/client (replacing _example); root vitest excludes integration tests; packages/_example is fully deleted. Workspace ready for the verification gates in Task 4.
  </done>
</task>

<task type="auto">
  <name>Task 4: Workspace verification gates — pnpm install / biome / tsc / build / test all green</name>
  <files>
    pnpm-lock.yaml (modified by `pnpm install` — committed alongside per lockfile-discipline rule)
    (no other files modified; this is a verification + lockfile-sync task)
  </files>
  <read_first>
    - .planning/phases/02-core-3-adoption-rename/02-RESEARCH.md §"Workspace Verification Gates — After 02-01" (the authoritative gate table)
    - .planning/phases/02-core-3-adoption-rename/02-PATTERNS.md Pattern H (pnpm install — lockfile commit discipline)
    - CLAUDE.md (user global rule: lockfile must commit alongside package.json)
  </read_first>
  <action>
    Run the Phase-2 workspace verification gates per RESEARCH §"Workspace Verification Gates — After 02-01" in this exact order; each must exit 0 before proceeding to Task 5 (commit):
    1. `pnpm install` (NOT `--frozen-lockfile` on the first run — the lockfile must update to reflect the new workspace package + removed jest/ts-jest/@types/jest devDeps + removed `_example`). After it succeeds, stage `pnpm-lock.yaml` for the same commit as the rest of the diff (lockfile-discipline).
    2. `pnpm biome check .` — zero violations across the whole workspace (D-03 noExplicitAny, noUnusedVariables, format checks).
    3. `pnpm tsc --noEmit` — zero typecheck errors across the whole workspace (strict + isolatedDeclarations + noUncheckedIndexedAccess).
    4. `pnpm build` — must produce `packages/client/dist/index.mjs` and `packages/client/dist/index.d.mts`.
    5. `pnpm test` — all `@godoo/client` unit tests pass (12 `*.test.ts` files); 10 `*.integration.test.ts` files are EXCLUDED (per Task 3 vitest exclude) — no failures, no errors.
    6. `pnpm install --frozen-lockfile` — must exit 0 (proves the lockfile staged in step 1 is in sync with package.json — this is what CI uses).
    If ANY gate fails: do NOT commit; iterate on the failing artifact (Task 2 / Task 3 / package.json) until all gates pass. Per debugging-discipline rule: read the full error, understand the cause, make ONE targeted fix, re-run gates.
  </action>
  <verify>
    All six gate commands above exit 0 in the listed order. `git status pnpm-lock.yaml` shows the lockfile modified and staged.
  </verify>
  <acceptance_criteria>
    - `pnpm install` exits 0; `pnpm-lock.yaml` is modified and staged
    - `pnpm biome check .` exits 0 with zero violations across the workspace
    - `pnpm tsc --noEmit` exits 0 from repo root
    - `pnpm build` exits 0 and `packages/client/dist/index.mjs` + `packages/client/dist/index.d.mts` both exist
    - `pnpm test` exits 0 with 12 client unit test files passing; integration test files NOT executed (excluded by root vitest config)
    - `pnpm install --frozen-lockfile` exits 0 (proves lockfile sync — CI gate)
    - `git diff --cached --name-only` includes both `packages/client/package.json` AND `pnpm-lock.yaml` (lockfile-discipline)
  </acceptance_criteria>
  <done>
    All workspace verification gates green; lockfile in sync; ready for the single adoption commit in Task 5.
  </done>
</task>

<task type="auto">
  <name>Task 5: Single adoption commit on develop referencing source SHA</name>
  <files>
    (commits all staged changes from Tasks 1-4 — packages/client/**, deleted packages/_example/**, tsconfig.json, vitest.config.ts, pnpm-lock.yaml)
  </files>
  <read_first>
    - .planning/phases/02-core-3-adoption-rename/02-CONTEXT.md D-01 (commit subject template)
    - ../godoo-hq/.planning/notes/godoo-adoption-protocol.md (single commit per adoption per protocol)
    - CLAUDE.md (user global git-workflow rule: develop is the default working branch; conventional commits)
  </read_first>
  <action>
    Confirm current branch is `develop` (per CLAUDE.md git-workflow rule: develop is the default working branch; never push to main directly). Stage all Tasks-1-through-4 changes if not already staged: `packages/client/**` (new), `packages/_example/**` (deleted), `tsconfig.json` (modified), `vitest.config.ts` (modified), `pnpm-lock.yaml` (modified). Create ONE commit (godoo-adoption protocol mandates single commit per package adoption) with subject EXACTLY matching this pattern (using the SOURCE_SHA captured in Task 0):
        `feat(client): adopt @marcfargas/odoo-client as @godoo/client (from odoo-toolbox@<SOURCE_SHA>)`
    Where `<SOURCE_SHA>` is the 10-character hex string from `git -C C:\dev\odoo-toolbox rev-parse --short=10 HEAD`. Commit body may include an itemized list of the toolchain conversion, strict-TS pass, integration-test skip, _example deletion, and link to CORE-01 + CORE-05. Do NOT push (push happens when the user is ready; CLAUDE.md git-workflow rule).
    Append a Co-Authored-By trailer per global rules:
        `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`
  </action>
  <verify>
    `git log -1 --format=%s` matches the regex above; `git log -1 --stat` shows packages/client/** additions, packages/_example/** deletions, tsconfig.json + vitest.config.ts + pnpm-lock.yaml modifications.
  </verify>
  <acceptance_criteria>
    - `git rev-parse --abbrev-ref HEAD` outputs `develop`
    - `git log -1 --format=%s` matches the regex `^feat\(client\): adopt @marcfargas/odoo-client as @godoo/client \(from odoo-toolbox@[a-f0-9]{7,40}\)$`
    - `git log -1 --name-only` includes `packages/client/package.json`, `packages/client/tsconfig.json`, `packages/client/tsdown.config.ts`, `tsconfig.json`, `vitest.config.ts`, `pnpm-lock.yaml`, AND lists `packages/_example/*` files as deleted
    - `git log -1 --format=%b` contains `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`
    - Working tree clean after commit: `git status --porcelain` returns empty
    - The commit lives on `develop`, NOT pushed (no instruction to push in this plan)
  </acceptance_criteria>
  <done>
    Single feat(client) commit on develop wraps up Plan 02-01; @godoo/client is the first real package in godoo-ts; CORE-01 satisfied (unit tests pass, integration deferred); CORE-05 partially satisfied (zero @marcfargas/odoo-client refs in packages/client/).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source-repo → godoo-ts working tree | File whitelist (Task 1) — only `src/`, `tests/`, `README.md`, `LICENSE` cross the boundary; everything else (dist/, node_modules/, CHANGELOG, examples, build artifacts) is rejected |
| package manifest → npm supply chain | `pnpm install` (Task 4) resolves only `debug` and `@types/debug` for this package — no NEW external dependencies introduced in this plan (jest/ts-jest/@types/jest are REMOVED, shrinking the supply-chain surface) |
| developer machine → public GitHub | Adoption commit (Task 5) is local-only on `develop`; push happens later per user instruction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | Whitelist copy in Task 1 — risk of accidentally copying source-repo `dist/`, `tsconfig.tsbuildinfo`, or `CHANGELOG.md` (carries stale @marcfargas refs) | mitigate | Acceptance criteria explicitly check non-existence of `dist/`, `node_modules/`, `CHANGELOG.md`, `tsconfig.tsbuildinfo`. Task 1 uses an explicit whitelist, NOT a blacklist. |
| T-02-01-02 | Information Disclosure | Strict-TS pass touches RPC types; risk of inadvertently exposing internal types or leaking credentials in error messages | accept | No credentials handled in this plan (integration tests skipped). Error narrowing in `src/types/errors.ts` (RESEARCH §V7) is preserved during the strict-TS pass; no API changes per CONTEXT.md out-of-scope rule. |
| T-02-01-03 | Tampering | `pnpm install` (Task 4) — possible postinstall script in transitively added dependency | mitigate | pnpm 11 `allowBuilds` in `pnpm-workspace.yaml` already restricts to `lefthook` (Phase-1 D-03). Any new postinstall trigger prompts. No new external deps in this plan (only `debug` + `@types/debug` already present in source). |
| T-02-01-SC | Tampering (supply chain) | npm install of `debug ^4.4.3` + `@types/debug ^4.1.12` (both already present in source manifest, already in lockfile from Phase 1 workspace-root devDeps — and pinned via the workspace pnpm minimumReleaseAge policy) | mitigate | RESEARCH.md §"Package Legitimacy Audit" is not present in RESEARCH.md because no NEW packages are introduced in this plan — both `debug` and `@types/debug` are well-known long-established packages (debug is a top-50 npm package with >10y history; @types/debug is from DefinitelyTyped). No `[ASSUMED]`/`[SUS]`/`[SLOP]` flags. No additional human-verify checkpoint required. If a fresh-install transitively pulls a new postinstall (T-02-01-03), the pnpm allowBuilds gate catches it. |
| T-02-01-04 | Denial of Service | A failing strict-TS gate (Task 4 step 3) could leave the workspace in a non-green state mid-task | mitigate | Task 4 ordering: lockfile sync → biome → tsc → build → test → frozen-lockfile sync. Each gate must pass before next; do NOT commit until all six pass. Debugging-discipline rule applies (one targeted fix per failure). |
| T-02-01-05 | Repudiation | Adoption commit subject must reference source SHA so future archaeology can trace history (per godoo-adoption-protocol step 4) | mitigate | Task 5 acceptance criterion regex enforces the exact subject pattern including `(from odoo-toolbox@<SHA>)`. SOURCE_SHA captured in Task 0 and reused in Task 5. |

No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages → no blocking-human checkpoint required (none of `debug`/`@types/debug` are newly-introduced; both inherited).
</threat_model>

<verification>
After Task 5 completes (and before declaring the plan done), run these checks once more to confirm the After-02-01 gate table from RESEARCH.md §"Workspace Verification Gates":

- `pnpm install --frozen-lockfile && pnpm biome check . && pnpm tsc --noEmit && pnpm build && pnpm test` all exit 0 from a clean checkout
- `test ! -d packages/_example` passes
- `git grep "@marcfargas/odoo-client" packages/client/` returns zero matches
- `git log -1 --format=%s` matches the feat(client) regex above

If/when the commit is pushed (per user instruction): `gh run watch <run-id>` must show `ci (22)` and `ci (24)` both succeeding (no new integration job yet — that's 02-03).
</verification>

<success_criteria>
- Plan 02-01 lands as ONE commit on `develop` with the feat(client) subject referencing the source SHA
- `packages/client/` exists with renamed `@godoo/client` manifest, tsdown ESM-only output, per-package tsconfig extending workspace base
- `packages/_example/` is deleted
- `Domain`, `DomainClause`, `RpcArg` types exported from `@godoo/client` package root
- 10 client integration test files are `describe.skip`-wrapped with `// TODO(CORE-03)` markers
- Workspace gates green: `pnpm biome check .`, `pnpm tsc --noEmit`, `pnpm build`, `pnpm test`, `pnpm install --frozen-lockfile` all exit 0
- Zero `@marcfargas/odoo-client` strings in `packages/client/`
- CORE-01 satisfied (unit tests pass, integration skipped); CORE-05 partially satisfied (client side)
- `pre-adoption-baseline` tag exists on `odoo-toolbox` HEAD
</success_criteria>

<output>
On completion, create `.planning/phases/02-core-3-adoption-rename/02-01-SUMMARY.md` per the standard summary template:
- What was built (concrete file list)
- Source SHA referenced
- Strict-TS pass outcomes (any count before/after, types added)
- Verification gate results
- Hand-off notes for 02-02 (Domain / RpcArg are now importable from @godoo/client)
</output>
