---
phase: 02-core-3-adoption-rename
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - packages/client/tests/integration-setup.ts
  - packages/client/vitest.integration.config.ts
  - packages/client/package.json
  - packages/client/tests/**/*.integration.test.ts
  - packages/testcontainers/package.json
  - package.json
  - .github/workflows/ci.yml
  - pnpm-lock.yaml
autonomous: false
requirements:
  - CORE-03
user_setup: []
tags:
  - typescript
  - vitest
  - testcontainers
  - docker
  - ci
  - github-actions
  - branch-protection

must_haves:
  truths:
    - "T7: 10 client integration tests are re-enabled (no describe.skip, no // TODO(CORE-03) markers) and pass locally against @godoo/testcontainers' startOdoo()"
    - "T8: A new `integration` CI job exists in .github/workflows/ci.yml with Node 22 + 24 matrix; triggers on push to develop/main + PRs targeting either branch; require-ci-on-main ruleset's required_status_checks list includes `integration (22)` and `integration (24)`"
    - "T6: pnpm biome check ., pnpm tsc --noEmit, pnpm build, pnpm test (unit) all exit 0 across workspace; new gate: `pnpm test:integration` exits 0 locally"
    - "T10: Wiring commit subject matches: feat(client): re-enable integration tests against @godoo/testcontainers (CORE-03)"
    - "Container orchestration is entirely via @godoo/testcontainers (D-05) — no services: block, no docker-compose.test.yml"
  artifacts:
    - path: "packages/client/tests/integration-setup.ts"
      provides: "vitest globalSetup wiring startOdoo() into process.env.ODOO_URL / DB_NAME / DB_USER / DB_PASSWORD (and the alternative ODOO_HOST / DB / USER / PASSWORD names if any test reads them)"
      contains: "startOdoo"
    - path: "packages/client/vitest.integration.config.ts"
      provides: "Per-package vitest integration config with globalSetup, 600s timeouts, integration include pattern, single-fork to share one Odoo container"
      contains: "globalSetup"
    - path: ".github/workflows/ci.yml"
      provides: "New `integration` job (Node 22+24 matrix) alongside existing `ci` job; PR trigger widened to include develop"
      contains: "integration:"
  key_links:
    - from: "packages/client/vitest.integration.config.ts"
      to: "packages/client/tests/integration-setup.ts"
      via: "globalSetup"
      pattern: 'globalSetup.*integration-setup'
    - from: "packages/client/tests/integration-setup.ts"
      to: "@godoo/testcontainers"
      via: "import { startOdoo }"
      pattern: "from '@godoo/testcontainers'"
    - from: ".github/workflows/ci.yml integration job"
      to: "@godoo/testcontainers + @godoo/client"
      via: "pnpm test:integration"
      pattern: 'pnpm test:integration'
    - from: "require-ci-on-main ruleset"
      to: "integration (22), integration (24)"
      via: "required_status_checks"
      pattern: 'integration \(2[24]\)'
---

<objective>
**Phase Goal (user story — repeated in every Phase-2 plan for context):**

**As a** TypeScript developer adopting the godoo core libraries, **I want to** install `@godoo/client`, `@godoo/testcontainers`, and `@godoo/introspection` from the public `@godoo/` scope (with no `@marcfargas/odoo-*` strings left in the source) **so that** I can manage real Odoo ERP instances against a single canonical TS home that builds, typechecks, lints, and tests green on Node 22 + 24.

**This plan (02-03):** Wire `@godoo/testcontainers` as the `globalSetup` source for `@godoo/client`'s integration tests. Create `packages/client/tests/integration-setup.ts` (calls `startOdoo()`, injects `process.env.ODOO_URL` etc.). Create `packages/client/vitest.integration.config.ts` (globalSetup, 600s timeouts, single-fork). Add `test:integration` scripts at workspace root + per-package. Delete the 10 `describe.skip(...)` wrappers and `// TODO(CORE-03)` markers introduced in Plan 02-01. Add a new `integration` CI job to `.github/workflows/ci.yml` (Node 22 + 24 matrix, mirrors existing `ci` job structure). Widen `pull_request.branches` from `[main]` to `[main, develop]` per D-04. Update the `require-ci-on-main` GitHub branch ruleset to add `integration (22)` and `integration (24)` to required status checks (blocking-human checkpoint via `gh api ... --input -`). Single commit.

Purpose: Closes CORE-03 by making the client's full integration test suite green against the adopted `@godoo/testcontainers` (not the source-repo's raw `testcontainers`-based globalSetup, per D-05). Lands the Docker/Odoo CI infrastructure deferred from Phase 1 (Phase-1 D-09 carry-forward). This is the only plan with a non-trivial CI surface area.

Output: One commit on `develop` containing globalSetup + integration vitest config + script wiring + CI workflow changes + lockfile. Separate manual action via `gh` CLI to update branch protection (a `checkpoint:human-verify` task).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-3-adoption-rename/02-CONTEXT.md
@.planning/phases/02-core-3-adoption-rename/02-RESEARCH.md
@.planning/phases/02-core-3-adoption-rename/02-PATTERNS.md
@.planning/phases/01-repo-toolchain-bootstrap/01-VERIFICATION.md
@.planning/phases/02-core-3-adoption-rename/02-01-PLAN.md
@.planning/phases/02-core-3-adoption-rename/02-02-PLAN.md
@CLAUDE.md

<interfaces>
Consumed from @godoo/testcontainers (adopted in 02-02):

  import { startOdoo, type StartedOdooContainer } from '@godoo/testcontainers';
  // startOdoo(options) returns Promise<StartedOdooContainer>
  // StartedOdooContainer exposes: .url, .database, and .cleanup()

Env-var contract preserved from source-repo integration tests (verified via grep in RESEARCH.md Assumption A5):

  process.env.ODOO_URL          // injected by globalSetup, read by every integration test
  process.env.ODOO_DB_NAME      // injected; alternative name ODOO_DB used elsewhere
  process.env.ODOO_DB_USER      // injected as 'admin' (test default)
  process.env.ODOO_DB_PASSWORD  // injected as 'admin' (test default)
  process.env.ODOO_USER         // alt name (used in examples.integration.test.ts but that file is in introspection — verify what's actually read by client integration tests)
  process.env.ODOO_PASSWORD     // alt name

Existing `ci` job structure in .github/workflows/ci.yml (mirrored for the new `integration` job — lines 8-25 of the file per PATTERNS.md):

  ci:
    runs-on: ubuntu-latest
    strategy:
      matrix: { node-version: [22, 24] }
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v4 with node-version + cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm biome check .
      - run: pnpm tsc --noEmit
      - run: pnpm build
      - run: pnpm test
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create packages/client/tests/integration-setup.ts (vitest globalSetup using @godoo/testcontainers.startOdoo)</name>
  <files>
    packages/client/tests/integration-setup.ts (NEW — no analog in repo per PATTERNS.md §No Analog Found)
  </files>
  <read_first>
    - .planning/phases/02-core-3-adoption-rename/02-RESEARCH.md §"Integration-Test Reactivation (CORE-03 mechanics) — Option A" (lines 254-271 — full template)
    - .planning/phases/02-core-3-adoption-rename/02-PATTERNS.md §packages/client/tests/integration-setup.ts (NEW, NO ANALOG)
    - .planning/phases/02-core-3-adoption-rename/02-RESEARCH.md Assumption A5 (env-var contract verification across all 10 client integration test files)
    - packages/client/tests/*.integration.test.ts (read 2-3 to confirm exact env-var names actually read — `grep -rn 'process.env\.' packages/client/tests/*.integration.test.ts`)
    - .planning/phases/02-core-3-adoption-rename/02-CONTEXT.md D-05 (container orchestration entirely via @godoo/testcontainers)
  </read_first>
  <behavior>
    - After this task: `packages/client/tests/integration-setup.ts` exports vitest globalSetup `setup` + `teardown` async functions.
    - `setup()` calls `startOdoo({ modules: ['base', 'mail', 'crm'] })` from `@godoo/testcontainers`, assigns the returned container's `url` / `database` / credentials to the env vars that the 10 integration test files actually read, and stores the container reference in module scope.
    - `teardown()` calls `container.cleanup()` if a container was started; safe to call when undefined.
    - The setup file is the SINGLE source of container lifecycle for the client's integration suite (D-05 — no `services:` block in CI, no docker-compose).
    - The 10 `*.integration.test.ts` files require zero per-test edits after this — they continue to read `process.env.ODOO_URL` etc. as they did in the source repo.
  </behavior>
  <action>
    Before writing the file: grep all 10 `packages/client/tests/**/*.integration.test.ts` for `process.env\.` references and enumerate the EXACT env-var names read. RESEARCH.md Assumption A5 lists the expected set (`ODOO_URL`, `ODOO_DB_NAME`, `ODOO_DB_USER`, `ODOO_DB_PASSWORD`) but executor must VERIFY rather than assume — if any test reads `ODOO_HOST` or `ODOO_DB` or alternative names, expand the env injection.

    Write `packages/client/tests/integration-setup.ts` per PATTERNS.md §packages/client/tests/integration-setup.ts (NEW, NO ANALOG) — concrete identifiers:
    - import `startOdoo` and the type `StartedOdooContainer` from `'@godoo/testcontainers'`
    - module-scope let binding `odoo: StartedOdooContainer | undefined`
    - export `async function setup(): Promise<void>` that:
        - calls `await startOdoo({ modules: ['base', 'mail', 'crm'] })` and assigns to `odoo`
        - sets `process.env.ODOO_URL = odoo.url`
        - sets `process.env.ODOO_DB_NAME = odoo.database`
        - sets `process.env.ODOO_DB_USER = 'admin'`
        - sets `process.env.ODOO_DB_PASSWORD = 'admin'`
        - sets any ADDITIONAL env vars surfaced by the grep above (e.g. `ODOO_HOST`, `ODOO_DB`, `ODOO_USER`, `ODOO_PASSWORD` if any client integration test reads them — verify, don't assume)
    - export `async function teardown(): Promise<void>` that calls `await odoo?.cleanup()`

    Per CLAUDE.md secrets-handling rule: do NOT log credentials. The setup function MUST NOT contain `console.log`/`console.error`/`debug` calls that include `ODOO_DB_PASSWORD` or `ODOO_DB_USER` values (verified in source repo's globalSetup — does not log creds; preserve this property). The values are `admin`/`admin` test defaults, not real secrets, but the pattern matters.

    Per D-03 strict-TS: explicit return type on `setup` and `teardown` (`Promise<void>`); no `any`; no `// @ts-expect-error`; no `// biome-ignore`. File must pass `pnpm tsc --noEmit` and `pnpm biome check .` after creation.
  </action>
  <verify>
    File exists; `grep -F "from '@godoo/testcontainers'" packages/client/tests/integration-setup.ts` exits 0; `grep -F "startOdoo" packages/client/tests/integration-setup.ts` exits 0; `grep -F "process.env.ODOO_URL" packages/client/tests/integration-setup.ts` exits 0; file passes `pnpm tsc --noEmit`.
  </verify>
  <acceptance_criteria>
    - `packages/client/tests/integration-setup.ts` exists
    - Contains `import { startOdoo, type StartedOdooContainer } from '@godoo/testcontainers';` (literal substring)
    - Exports `async function setup(): Promise<void>` (verify by `grep -E 'export\s+async\s+function\s+setup\s*\(\s*\)\s*:\s*Promise<void>' packages/client/tests/integration-setup.ts` exits 0)
    - Exports `async function teardown(): Promise<void>` (same grep shape)
    - Sets `process.env.ODOO_URL`, `process.env.ODOO_DB_NAME`, `process.env.ODOO_DB_USER`, `process.env.ODOO_DB_PASSWORD` (one grep per var; each exits 0)
    - For every additional `process.env.ODOO_*` name read by any of the 10 `*.integration.test.ts` files (enumerated in pre-task grep): the env var is set in `setup()` (acceptance is an executor-driven completeness check — the grep produced an enumeration; every name must appear in the setup file)
    - `pnpm tsc --noEmit` exits 0 over the workspace
    - `pnpm biome check packages/client/tests/integration-setup.ts` exits 0
    - `grep -E 'console\.(log|error|warn|info)' packages/client/tests/integration-setup.ts` returns zero matches (no credential logging risk)
  </acceptance_criteria>
  <done>
    integration-setup.ts wires @godoo/testcontainers.startOdoo() into process.env contract; passes strict-TS + biome; preserves no-logging convention.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create packages/client/vitest.integration.config.ts, add test:integration scripts, unskip the 10 integration test files</name>
  <files>
    packages/client/vitest.integration.config.ts (NEW)
    packages/client/package.json (modify — add `scripts.test:integration`)
    packages/testcontainers/package.json (modify — add `scripts.test:integration` for symmetry per RESEARCH §Workspace test:integration script)
    package.json (root, modify — add `scripts.test:integration` workspace orchestration)
    packages/client/tests/**/*.integration.test.ts (modify — delete the 10 `describe.skip` wrappers and the `// TODO(CORE-03)` comments introduced in Plan 02-01)
  </files>
  <read_first>
    - .planning/phases/02-core-3-adoption-rename/02-PATTERNS.md §packages/client/vitest.integration.config.ts (test-config, vitest-config — NEW in Plan 02-03)
    - .planning/phases/02-core-3-adoption-rename/02-RESEARCH.md §"Workspace `test:integration` script"
    - .planning/phases/02-core-3-adoption-rename/02-RESEARCH.md §"Integration-Test Reactivation Option A — vitest.integration.config.ts shape"
    - packages/client/tests/*.integration.test.ts (the 10 files needing unskip — grep for `describe.skip` + TODO comment to locate)
    - package.json (root — current scripts to add `test:integration` alongside)
  </read_first>
  <behavior>
    - After this task: `pnpm test:integration` from repo root invokes vitest with the per-package integration config; the 10 client integration tests + 3 testcontainers integration tests can be discovered and (if Docker is available locally) executed.
    - The 10 client `*.integration.test.ts` files no longer have `describe.skip(` wrappers or `// TODO(CORE-03)` markers (introduced in Plan 02-01).
    - `pnpm test` (unit) is UNAFFECTED — the root vitest config still excludes `*.integration.test.ts` (from Plan 02-01).
  </behavior>
  <action>
    Write `packages/client/vitest.integration.config.ts` per PATTERNS.md §packages/client/vitest.integration.config.ts. Concrete identifiers:
    - import `defineConfig` from `'vitest/config'`
    - export default `defineConfig({ test: { include: ['tests/**/*.integration.test.ts'], globalSetup: './tests/integration-setup.ts', testTimeout: 600_000, hookTimeout: 600_000, sequence: { concurrent: false }, pool: 'forks', fileParallelism: false } })`
    - Rationale (per PATTERNS.md): 600s timeout for cold Odoo container startup; single fork + no parallelism because a single Odoo container is shared across all integration files (parallel runs would race for the same DB).

    Add `scripts.test:integration` to `packages/client/package.json`: `"test:integration": "vitest run --config vitest.integration.config.ts"`.
    Add `scripts.test:integration` to `packages/testcontainers/package.json`: `"test:integration": "vitest run"` (per RESEARCH §Workspace test:integration script — testcontainers' default `test` script is already vitest run, but the alias keeps the root command uniform).
    Add `scripts.test:integration` to root `package.json`: `"test:integration": "pnpm --filter @godoo/client --filter @godoo/testcontainers run test:integration"` (per RESEARCH.md exact recommendation).

    Delete the integration-test skip wrappers introduced in Plan 02-01 across all 10 `packages/client/tests/**/*.integration.test.ts` files. Concretely: each file currently has, near the top, a `// TODO(CORE-03): re-enable after @godoo/testcontainers lands (Phase 02-03)` line immediately followed by `describe.skip(...)`. Delete the TODO comment line and rewrite `describe.skip(` → `describe(` (just remove `.skip`). Do NOT touch the test bodies — they continue to read `process.env.ODOO_URL` etc. (preserved by Task 1's globalSetup).
  </action>
  <verify>
    Confirm `packages/client/vitest.integration.config.ts` exists with `globalSetup` referencing `./tests/integration-setup.ts`. Confirm root `package.json`, `packages/client/package.json`, `packages/testcontainers/package.json` all have `scripts.test:integration`. Confirm `grep -c "describe.skip" packages/client/tests/*.integration.test.ts | awk -F: '{s+=$2} END {print s}'` outputs 0. Confirm `grep -c "TODO(CORE-03)" packages/client/tests/*.integration.test.ts | awk -F: '{s+=$2} END {print s}'` outputs 0.
  </verify>
  <acceptance_criteria>
    - `packages/client/vitest.integration.config.ts` exists
    - Contains `globalSetup: './tests/integration-setup.ts'` (literal substring)
    - Contains `testTimeout: 600_000` and `hookTimeout: 600_000`
    - Contains `pool: 'forks'` and `fileParallelism: false`
    - `jq -r '.scripts["test:integration"]' packages/client/package.json` outputs `vitest run --config vitest.integration.config.ts`
    - `jq -r '.scripts["test:integration"]' packages/testcontainers/package.json` outputs `vitest run`
    - `jq -r '.scripts["test:integration"]' package.json` outputs `pnpm --filter @godoo/client --filter @godoo/testcontainers run test:integration`
    - `grep -c "describe.skip" packages/client/tests/*.integration.test.ts | awk -F: '{s+=$2} END {print s}'` equals 0 (no skip wrappers remain)
    - `grep -c "TODO(CORE-03)" packages/client/tests/*.integration.test.ts | awk -F: '{s+=$2} END {print s}'` equals 0 (no markers remain)
    - `pnpm tsc --noEmit` exits 0
    - `pnpm biome check .` exits 0
  </acceptance_criteria>
  <done>
    vitest.integration.config.ts wired to globalSetup; test:integration scripts present at root + both packages; 10 client integration test files un-skipped and TODO-free.
  </done>
</task>

<task type="auto">
  <name>Task 3: Local integration test run — verify Docker availability and pnpm test:integration green</name>
  <files>
    (no files modified — runtime verification only)
  </files>
  <read_first>
    - .planning/phases/02-core-3-adoption-rename/02-RESEARCH.md §"Workspace Verification Gates — After 02-03"
    - .planning/phases/02-core-3-adoption-rename/02-RESEARCH.md §"Environment Availability" (Docker required on marcwin)
    - .planning/phases/02-core-3-adoption-rename/02-RESEARCH.md §"Container caching / startup time" (snapshot caching default-on)
  </read_first>
  <action>
    (a) Verify Docker is available locally on marcwin: `docker info` must exit 0. If Docker Desktop is not running, stop here and surface the blocker — the integration tests cannot run locally without it. (Fallback per RESEARCH §"Environment Availability": iteration via CI is possible, but local verification is strongly preferred before committing.)
    (b) Run `pnpm test:integration` from the workspace root. Expected: 10 client integration test files + 3 testcontainers integration test files execute and PASS. First run pulls Odoo + Postgres Docker images (~1.5 GB) and cold-inits Odoo (~3 minutes); subsequent runs benefit from `@godoo/testcontainers` snapshot caching (default-on per RESEARCH §Container caching). Total wallclock: ~10-15 minutes per Node version on first run; faster on cached runs.
    (c) After tests complete, verify Docker artifacts are cleaned: `docker ps -a` must NOT show leftover odoo/postgres containers (Ryuk cleanup default behavior per @godoo/testcontainers).
    (d) Re-run the unit gates from 02-02 to ensure no regression: `pnpm biome check . && pnpm tsc --noEmit && pnpm build && pnpm test` — all must still exit 0.
  </action>
  <verify>
    `docker info` exits 0; `pnpm test:integration` exits 0; `docker ps -a` shows no `odoo*` or `postgres*` containers (testcontainers Ryuk cleanup confirmed); regression unit gates still green.
  </verify>
  <acceptance_criteria>
    - `docker info` exits 0 (Docker available locally)
    - `pnpm test:integration` exits 0 with all 13 integration test files passing (10 client + 3 testcontainers)
    - `docker ps -a --filter 'name=odoo' --filter 'name=postgres'` returns no rows AFTER test completion (Ryuk cleanup verified)
    - `pnpm biome check .` exits 0
    - `pnpm tsc --noEmit` exits 0
    - `pnpm build` exits 0
    - `pnpm test` exits 0 (unit-only, integration tests excluded by root vitest)
  </acceptance_criteria>
  <done>
    Integration tests pass locally against @godoo/testcontainers; no leftover Docker artifacts; unit gates still green.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add `integration` job to .github/workflows/ci.yml; widen PR triggers; commit lockfile</name>
  <files>
    .github/workflows/ci.yml (modify — append `integration` job; widen `pull_request.branches` from `[main]` to `[main, develop]`)
    pnpm-lock.yaml (modified by any earlier `pnpm install` — committed alongside)
  </files>
  <read_first>
    - .github/workflows/ci.yml (current state — Phase-1 single `ci` job)
    - .planning/phases/02-core-3-adoption-rename/02-PATTERNS.md §.github/workflows/ci.yml (modified — add `integration` job, widen PR triggers)
    - .planning/phases/02-core-3-adoption-rename/02-RESEARCH.md §"Integration CI Workflow Design"
    - .planning/phases/02-core-3-adoption-rename/02-CONTEXT.md D-04 (per-push develop+main + PRs to either, no path filtering, no nightly cron) + D-05 (no services: block, no docker-compose)
  </read_first>
  <action>
    Edit `.github/workflows/ci.yml`:
    (a) `on.pull_request.branches`: currently `[main]` — change to `[main, develop]` (D-04 mandate). The `on.push.branches` is already `[main, develop]` from Phase 1 — leave as-is.
    (b) Append a new top-level job named EXACTLY `integration` (lowercase, no suffix) under `jobs:`. The job MUST mirror the existing `ci` job structure (RESEARCH.md §"Integration CI Workflow Design" + PATTERNS.md §.github/workflows/ci.yml) with these concrete attributes:
        - `runs-on: ubuntu-latest`
        - `strategy.matrix.node-version: [22, 24]`
        - Steps (in order):
            1. `actions/checkout@v4`
            2. `pnpm/action-setup@v6`
            3. `actions/setup-node@v4` with `node-version: ${{ matrix.node-version }}` and `cache: 'pnpm'`
            4. `pnpm install --frozen-lockfile`
            5. `pnpm build` (must run BEFORE pnpm test:integration — workspace symlink points at dist/ not src/ per RESEARCH §"pnpm build must run before pnpm test:integration")
            6. `pnpm test:integration` with `env.TESTCONTAINERS_RYUK_DISABLED: 'false'` (matches source-repo CI; Ryuk cleans orphan containers)
    (c) Do NOT add a `services:` block (D-05 — container orchestration is entirely via @godoo/testcontainers); do NOT create a `docker-compose.test.yml`; do NOT add path filters or schedule (nightly) triggers (D-04 — same trigger surface as `ci` job, no extras).

    Job-name discipline (Phase-1 landmine #8 + RESEARCH §"Branch protection ruleset update"): the matrix produces required check names `integration (22)` and `integration (24)`. These MUST be added to the ruleset in Task 5. A typo here silently makes the check non-required — confirm the job name in the yaml is exactly `integration` (no quotes, no suffix).

    Stage `.github/workflows/ci.yml` and any pending `pnpm-lock.yaml` changes from earlier tasks.
  </action>
  <verify>
    `grep -E "^\s+integration:" .github/workflows/ci.yml` exits 0; `grep -F "node-version: [22, 24]" .github/workflows/ci.yml` returns at least 2 matches (one for `ci`, one for `integration`); `grep -F "pnpm test:integration" .github/workflows/ci.yml` exits 0; `grep -F "TESTCONTAINERS_RYUK_DISABLED" .github/workflows/ci.yml` exits 0; `grep -E "pull_request:" -A 2 .github/workflows/ci.yml` shows branches list includes `develop`.
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a top-level job named `integration` (regex `^\s\sintegration:` exits 0 with 1 match; indentation matches existing `ci:` job)
    - Job has `strategy.matrix.node-version: [22, 24]` (yaml-compatible exact string)
    - Job has step `pnpm test:integration` with `env.TESTCONTAINERS_RYUK_DISABLED: 'false'` (yaml string-or-quoted-string)
    - Job has step `pnpm build` BEFORE `pnpm test:integration`
    - Job has NO `services:` block (`grep -B 1 -A 1 "services:" .github/workflows/ci.yml` exits 1)
    - `pull_request.branches` includes both `main` AND `develop` (`grep -A 2 "pull_request:" .github/workflows/ci.yml | grep -E "main.*develop|develop.*main|- main$|- develop$"` finds matches)
    - File still validates as valid YAML (`pnpm dlx yaml-lint .github/workflows/ci.yml` or simple python yaml.safe_load — executor's choice; if no validator available, manual visual inspection vs. existing `ci` job suffices)
    - `pnpm-lock.yaml` (if modified anywhere in this plan) is staged alongside
  </acceptance_criteria>
  <done>
    CI workflow has new `integration` job (Node 22+24) mirroring `ci` job structure; PR trigger widened to include develop; no services: block; ready for branch protection update in Task 5.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Update require-ci-on-main branch ruleset to add integration (22) and integration (24) to required_status_checks (CHECKPOINT)</name>
  <files>
    (no local files modified - this task mutates server-side GitHub repository configuration: the require-ci-on-main ruleset's required_status_checks list)
  </files>
  <read_first>
    - .planning/phases/02-core-3-adoption-rename/02-RESEARCH.md section "Branch protection ruleset update" (recommended "gh api ... --input -" invocation)
    - .planning/phases/01-repo-toolchain-bootstrap/01-VERIFICATION.md (Phase-1 D-09 set required status checks to "ci (22)" + "ci (24)" + "non_fast_forward"; deviation #6 noted that "gh api --field" does not handle complex JSON - use "--input -")
    - .planning/phases/02-core-3-adoption-rename/02-CONTEXT.md D-04 (integration CI job blocks merges per same trigger surface as the existing ci job)
    - CLAUDE.md (secrets-handling rule: never read the gh auth token; verify auth via "gh auth status" only)
  </read_first>
  <action>
    CHECKPOINT (blocking-human): this task requires "gh" CLI authentication and modifies live GitHub repository configuration; branch-protection mistakes can lock the maintainer out of merging. Pause and surface the planned API mutation to the user before invoking.

    Step 1 - confirm auth: run "gh auth status" - it must report a logged-in user with admin access to godoo-dev/godoo-ts. Do NOT print or capture the auth token (CLAUDE.md secrets-handling rule).

    Step 2 - read the current ruleset to capture existing rules so the PUT does not drop them. Run "gh api repos/godoo-dev/godoo-ts/rulesets --jq '.[] | select(.name==\"require-ci-on-main\")'" and record the ruleset ID and the full rules array. The Phase-1 baseline (per 01-VERIFICATION.md) is: required_status_checks = "ci (22)", "ci (24)"; plus a "non_fast_forward" rule; enforcement = "active".

    Step 3 - construct a JSON payload (write to a temp file named update-ruleset.json at the repo root, NOT staged) that PRESERVES every existing rule and EXTENDS the required_status_checks list with two new entries: "integration (22)" and "integration (24)". End state: required_status_checks contains exactly four contexts ("ci (22)", "ci (24)", "integration (22)", "integration (24)"); the "non_fast_forward" rule is retained; enforcement remains "active".

    Step 4 - apply the update via stdin (NOT --field, per RESEARCH.md Phase-1 deviation #6 and landmine #9). Concretely run: "gh api repos/godoo-dev/godoo-ts/rulesets/RULESET_ID -X PUT --input update-ruleset.json" (substitute the ID from Step 2). After a successful response, delete update-ruleset.json (the payload contains no secrets but is transient).

    Step 5 - post-update verification (re-read and confirm three properties):
    (a) "gh api repos/godoo-dev/godoo-ts/rulesets/RULESET_ID --jq '.rules[] | select(.type==\"required_status_checks\") | .parameters.required_status_checks[].context'" MUST list all four contexts: "ci (22)", "ci (24)", "integration (22)", "integration (24)" (order may vary).
    (b) "gh api repos/godoo-dev/godoo-ts/rulesets/RULESET_ID --jq '.enforcement'" MUST output "active".
    (c) "gh api repos/godoo-dev/godoo-ts/rulesets/RULESET_ID --jq '.rules[] | select(.type==\"non_fast_forward\") | .type'" MUST output "non_fast_forward" (rule still present).

    DO NOT loosen any existing rule. DO NOT delete any required status check. DO NOT disable non_fast_forward. If any step fails or the ruleset shape is unexpected, STOP and surface to the user before continuing to Task 6 - landing the integration CI job without matching branch protection would make the new checks non-required-but-running, defeating the gate.

    Resume-signal (human checkpoint): once Step 5 verification passes, the user types "approved" to advance to Task 6. If "gh" auth is broken or the user declines, the user describes the issue or an alternative path (e.g. defer the ruleset update to a follow-up plan, in which case Task 6 acceptance criterion for the ruleset is documented as waived until the deferred plan lands).
  </action>
  <verify>
    Re-run the three post-update checks from Step 5 (parts a, b, c). All three must pass before the human types the resume-signal.
  </verify>
  <acceptance_criteria>
    - "gh auth status" exits 0 (auth confirmed; token value NOT read)
    - "gh api repos/godoo-dev/godoo-ts/rulesets --jq '.[] | select(.name==\"require-ci-on-main\") | .id'" returns a numeric ruleset ID (proves the Phase-1 ruleset still exists)
    - After the PUT: the required_status_checks contexts query (Step 5 part a) outputs exactly four lines: "ci (22)", "ci (24)", "integration (22)", "integration (24)"
    - The enforcement query (Step 5 part b) outputs the string "active"
    - The non_fast_forward query (Step 5 part c) outputs the string "non_fast_forward"
    - No local file modifications introduced by this task (temp update-ruleset.json deleted after use; never staged)
    - User explicitly typed "approved" resume-signal before Task 6 starts
  </acceptance_criteria>
  <done>
    Branch ruleset "require-ci-on-main" now requires "ci (22)", "ci (24)", "integration (22)", "integration (24)" while preserving "non_fast_forward" and "enforcement: active". The new integration job will block merges to main alongside the existing unit-test jobs. The user explicitly approved before Task 6 commits.
  </done>
  <resume-signal>Type "approved" once Step 5 verification passes (or describe issues / propose alternative such as deferring the ruleset update to a follow-up plan).</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Workspace verification gates + single CORE-03 wiring commit on develop</name>
  <files>
    (commits all staged changes from Tasks 1-5: integration-setup.ts, vitest.integration.config.ts, package.json[root + 2 packages], 10 *.integration.test.ts un-skip edits, .github/workflows/ci.yml, pnpm-lock.yaml. Branch ruleset update from Task 5 is server-side and not part of the commit.)
  </files>
  <read_first>
    - .planning/phases/02-core-3-adoption-rename/02-RESEARCH.md §"Workspace Verification Gates — After 02-03"
    - .planning/phases/02-core-3-adoption-rename/02-CONTEXT.md D-01 (commit subject template) — note: this is a wiring commit, not a per-package adoption commit, so the subject differs from the `feat(<pkg>): adopt ...` template; RESEARCH §"Plan 02-03 commit" recommends `feat(client): re-enable integration tests against @godoo/testcontainers (CORE-03)`.
    - CLAUDE.md (lockfile-discipline rule)
  </read_first>
  <action>
    Run the After-02-03 verification gates in order; each must exit 0:
    1. `pnpm install --frozen-lockfile` (CI gate sync).
    2. `pnpm biome check .` (zero violations).
    3. `pnpm tsc --noEmit` (zero typecheck errors).
    4. `pnpm build` (produces dist/ for both adopted packages).
    5. `pnpm test` (unit-only — 12 client unit + 3 testcontainers unit = 15 test files pass; integration excluded by root vitest config).
    6. `pnpm test:integration` (full integration suite passes — verified locally in Task 3; re-run if state changed since Task 3).
    7. `grep -c "describe.skip" packages/client/tests/*.integration.test.ts | awk -F: '{s+=$2} END {print s}'` outputs 0.
    8. `grep -rn "CORE-03" packages/client/tests/` returns 0 matches (TODO markers removed).
    9. `gh workflow view CI --yaml | grep -c "^  integration:"` outputs 1 (job exists; via `gh` to canonicalize the workflow as GitHub sees it).
    10. Confirm branch ruleset update (Task 5) succeeded by re-reading: `gh api repos/godoo-dev/godoo-ts/rulesets/<id> --jq '.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context'` — must show all four names.

    Confirm current branch is `develop`. Stage all Tasks-1-through-4 changes. Create ONE commit with subject EXACTLY (per RESEARCH §"Plan 02-03 commit"):
        `feat(client): re-enable integration tests against @godoo/testcontainers (CORE-03)`
    Commit body itemizes: globalSetup wiring via startOdoo(); 10 integration test files unskipped (TODO(CORE-03) markers removed); per-package vitest.integration.config.ts with 600s timeouts + single-fork; test:integration scripts at root + both packages; new `integration` CI job (Node 22+24, mirrors `ci`); PR trigger widened to include develop; branch ruleset updated server-side (Task 5). Append `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`. Do NOT push (per CLAUDE.md git-workflow; push happens on user instruction).
  </action>
  <verify>
    All 10 gates above exit 0; commit subject matches the regex; commit is on develop, not pushed.
  </verify>
  <acceptance_criteria>
    - `pnpm install --frozen-lockfile` exits 0
    - `pnpm biome check .` exits 0
    - `pnpm tsc --noEmit` exits 0
    - `pnpm build` exits 0; both `packages/client/dist/index.mjs` and `packages/testcontainers/dist/index.mjs` exist
    - `pnpm test` exits 0 (15 unit test files pass; integration excluded)
    - `pnpm test:integration` exits 0 (13 integration test files pass)
    - `grep -c "describe.skip" packages/client/tests/*.integration.test.ts | awk -F: '{s+=$2} END {print s}'` outputs 0
    - `grep -rn "CORE-03" packages/client/tests/` returns 0 matches
    - `gh workflow view CI --yaml | grep -c "^  integration:"` outputs 1
    - Branch ruleset required_status_checks list contains `ci (22)`, `ci (24)`, `integration (22)`, `integration (24)` (verified via `gh api` per Task 5)
    - `git rev-parse --abbrev-ref HEAD` outputs `develop`
    - `git log -1 --format=%s` matches `^feat\(client\): re-enable integration tests against @godoo/testcontainers \(CORE-03\)$`
    - `git log -1 --name-only` lists `packages/client/tests/integration-setup.ts`, `packages/client/vitest.integration.config.ts`, `packages/client/package.json`, `packages/testcontainers/package.json`, `package.json`, `.github/workflows/ci.yml`, all 10 `packages/client/tests/*.integration.test.ts`, `pnpm-lock.yaml`
    - `git log -1 --format=%b` contains `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`
    - `git status --porcelain` is empty after commit
  </acceptance_criteria>
  <done>
    CORE-03 closed: client integration tests pass against @godoo/testcontainers; new integration CI job + matching branch protection in place; single feat(client) wiring commit on develop.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → Docker daemon | `@godoo/testcontainers` drives Docker via host daemon (D-05); no privileged operations beyond standard testcontainers usage |
| CI runner → Docker images | Pulls `odoo:17/18/19` and `postgres:15-alpine` from Docker Hub at runtime; CI runner is GitHub-managed (`ubuntu-latest`) |
| Test process → process.env | globalSetup mutates `process.env` (vitest pattern); only test-default `admin/admin` creds — not real secrets |
| GitHub API → repo branch ruleset | Task 5 modifies `require-ci-on-main` ruleset via authenticated `gh` (admin permission required); preserves existing rules (non_fast_forward kept) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | Branch ruleset update (Task 5) could accidentally DROP existing required checks (`ci (22)`, `ci (24)`, `non_fast_forward`) | mitigate | Blocking-human checkpoint (Task 5); how-to-verify mandates reading existing rules first, then PATCHing to APPEND not REPLACE; post-update verification confirms all four checks present + non_fast_forward retained + enforcement still active. Per CLAUDE.md secrets-handling: do NOT read auth tokens; use `gh` indirection. |
| T-02-03-02 | Information Disclosure | `process.env.ODOO_DB_PASSWORD` injected by globalSetup risks being printed in test output | mitigate | Task 1 acceptance criterion: zero `console.*` calls in integration-setup.ts. Test files inherit no-logging pattern from source repo. Test-only creds (`admin/admin`) — not real secrets per RESEARCH §V8. |
| T-02-03-03 | Denial of Service | Container startup races between parallel test files could deadlock the suite or exhaust Docker resources | mitigate | vitest.integration.config.ts: `sequence.concurrent: false` + `pool: 'forks'` + `fileParallelism: false` — single Odoo container shared across all integration files; testTimeout/hookTimeout: 600_000 prevents premature aborts (RESEARCH §Pitfall 7). |
| T-02-03-04 | Denial of Service | Docker containers leaked from test runs could exhaust dev machine or CI runner resources | mitigate | `TESTCONTAINERS_RYUK_DISABLED: 'false'` in CI env (Task 4 acceptance); local Task 3 verifies `docker ps -a` clean after test completion. |
| T-02-03-05 | Elevation of Privilege | GitHub Actions workflow could be modified to leak repo secrets via PR builds | accept | Phase-2 scope adds no GitHub secrets; integration job needs only Docker + Node 22/24 + pnpm — no secrets injected. PR triggers expanded only to add `develop` per D-04; no `pull_request_target` (which would expose secrets to fork PRs). Workflow inherits Phase-1 default-permissions lockdown. |
| T-02-03-06 | Tampering | Job name typo in `.github/workflows/ci.yml` would silently make `integration (22)` / `integration (24)` checks non-required even after Task 5 ruleset update | mitigate | Task 4 acceptance criterion: regex matches `^\s\sintegration:` exactly; Task 6 acceptance: `gh workflow view CI --yaml | grep -c "^  integration:"` outputs 1. Cross-verified against ruleset required_status_checks list. |
| T-02-03-SC | Tampering (supply chain) | No new packages introduced in this plan — only NEW source file (integration-setup.ts) and a NEW config file (vitest.integration.config.ts), both consuming already-installed deps (`@godoo/testcontainers` via workspace:*, `vitest` from workspace root) | accept | No supply-chain surface added. `@godoo/testcontainers` is in-monorepo via workspace:*; `vitest` is pinned at workspace root from Phase 1. No `[ASSUMED]`/`[SUS]`/`[SLOP]` flags. |

No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages → no blocking-human checkpoint for package legitimacy (the only blocking-human checkpoint in this plan is Task 5 for branch protection, which is a different threat class).
</threat_model>

<verification>
After Task 6 commits, RESEARCH.md §"Workspace Verification Gates — After 02-03" must all pass:
- All After-02-01 + After-02-02 gates still pass
- `pnpm test:integration` exits 0 (13 integration test files pass)
- Zero `describe.skip` in client integration tests; zero CORE-03 TODOs
- Docker artifacts cleaned (`docker ps -a` shows no leftover odoo/postgres containers)
- `gh workflow view CI --yaml | grep -c "^  integration:"` outputs 1
- Branch protection includes all four required checks
- `git log -1 --format=%s` matches the feat(client) re-enable regex

CI verification (after push, per user instruction): all four jobs (`ci (22)`, `ci (24)`, `integration (22)`, `integration (24)`) green on develop.
</verification>

<success_criteria>
- Plan 02-03 lands as ONE commit on `develop` with feat(client) CORE-03 re-enable subject
- `packages/client/tests/integration-setup.ts` wires startOdoo() into process.env contract
- `packages/client/vitest.integration.config.ts` defines globalSetup + 600s timeouts + single-fork
- `test:integration` scripts at root + both packages
- 10 client `*.integration.test.ts` files un-skipped (zero describe.skip, zero CORE-03 TODOs)
- `integration` CI job in `.github/workflows/ci.yml` (Node 22+24 matrix, mirrors `ci`)
- `pull_request.branches` widened to `[main, develop]`
- `require-ci-on-main` ruleset's required_status_checks list updated to include `integration (22)` + `integration (24)`
- Workspace gates green incl. new `pnpm test:integration`
- CORE-03 satisfied
</success_criteria>

<output>
On completion, create `.planning/phases/02-core-3-adoption-rename/02-03-SUMMARY.md`:
- Files created (integration-setup.ts, vitest.integration.config.ts)
- Files modified (3 package.json files, .github/workflows/ci.yml, 10 integration test files)
- Env-var contract enumerated (which process.env.* names were injected, based on the pre-Task-1 grep)
- Integration test results (wallclock, pass/fail counts per Node version if CI run captured)
- Ruleset PATCH outcome (4 required checks confirmed)
- Hand-off notes for 02-04 (introspection adoption — no new integration infrastructure required; introspection's `examples.integration.test.ts` will be skipped per RESEARCH Open Question #3 recommendation)
</output>
