---
phase: 03-publishing-source-repo-shedding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _stubs/@godoo-client-stub/package.json
  - _stubs/@godoo-client-stub/index.js
  - _stubs/@godoo-introspection-stub/package.json
  - _stubs/@godoo-introspection-stub/index.js
  - _stubs/@godoo-testcontainers-stub/package.json
  - _stubs/@godoo-testcontainers-stub/index.js
autonomous: false
requirements:
  - PUB-01

must_haves:
  truths:
    - "@godoo-dev/client@0.0.0, @godoo-dev/introspection@0.0.0, and @godoo-dev/testcontainers@0.0.0 are live on the npm registry under the @godoo-dev scope"
    - "Each stub throws on require — no consumer can accidentally import it"
    - "The release/stub-bootstrap branch exists locally and is not pushed to origin (D-02)"
  artifacts:
    - path: "_stubs/@godoo-client-stub/package.json"
      provides: "Stub manifest for @godoo-dev/client@0.0.0"
      contains: '"version": "0.0.0"'
    - path: "_stubs/@godoo-client-stub/index.js"
      provides: "throw-on-require entrypoint"
      contains: "throw new Error"
    - path: "_stubs/@godoo-introspection-stub/package.json"
      provides: "Stub manifest for @godoo-dev/introspection@0.0.0"
    - path: "_stubs/@godoo-testcontainers-stub/package.json"
      provides: "Stub manifest for @godoo-dev/testcontainers@0.0.0"
  key_links:
    - from: "_stubs/@godoo-client-stub/"
      to: "npm registry @godoo-dev/client@0.0.0"
      via: "npm publish --access public"
      pattern: "npm view @godoo-dev/client version"
---

<objective>
Publish three throw-on-require stub packages at version 0.0.0 to anchor the @godoo-dev npm scope on the registry so that trusted publisher configuration (Plan 03-02) has a target package to bind to.

Purpose: npm trusted publishing requires a package to already exist on the registry before you can configure a trusted publisher for it. These stubs claim the package names at the @godoo-dev scope and prevent name-squatting.

Output: @godoo-dev/client@0.0.0, @godoo-dev/introspection@0.0.0, @godoo-dev/testcontainers@0.0.0 on npm (stub only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-publishing-source-repo-shedding/03-CONTEXT.md
@.planning/phases/03-publishing-source-repo-shedding/03-RESEARCH.md
@.planning/phases/03-publishing-source-repo-shedding/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stub packages on release/stub-bootstrap branch</name>
  <files>
    _stubs/@godoo-client-stub/package.json
    _stubs/@godoo-client-stub/index.js
    _stubs/@godoo-introspection-stub/package.json
    _stubs/@godoo-introspection-stub/index.js
    _stubs/@godoo-testcontainers-stub/package.json
    _stubs/@godoo-testcontainers-stub/index.js
  </files>
  <read_first>
    - packages/client/package.json — source the exact shape for license, author, repository, publishConfig fields
    - .planning/phases/03-publishing-source-repo-shedding/03-CONTEXT.md — stub constraints from Claude's Discretion block
    - .planning/phases/03-publishing-source-repo-shedding/03-PATTERNS.md — "Stub package.json files x3" section and "Stub index.js shape"
    - .planning/phases/03-publishing-source-repo-shedding/03-RESEARCH.md — Pattern 2 (stub package shape) and Pitfall 7 (stubs must NOT be inside pnpm workspace)
    - pnpm-workspace.yaml — confirm it includes 'packages/*' so _stubs/ at repo root is outside the workspace glob
  </read_first>
  <action>
    Create a local branch named release/stub-bootstrap from develop.

    Under the repo root, create a _stubs/ directory (NOT under packages/ — pnpm-workspace.yaml includes 'packages/*' which would cause changesets to discover the stubs). Create three subdirectories: @godoo-client-stub/, @godoo-introspection-stub/, @godoo-testcontainers-stub/.

    Each subdirectory needs a package.json and an index.js.

    package.json shape for each stub (per D-02 and PATTERNS.md):
    - "name": "@godoo-dev/client" (or /introspection, /testcontainers)
    - "version": "0.0.0"
    - "description": "Placeholder — install the latest release"
    - "license": "LGPL-3.0"
    - "author": "Marc Fargas <marc@marcfargas.com>"
    - "main": "./index.js"
    - "repository": { "type": "git", "url": "https://github.com/godoo-dev/godoo-ts.git", "directory": "packages/client" } (adjust directory per package: client/introspection/testcontainers)
    - "publishConfig": { "access": "public" }
    No "type": "module", no "exports", no "scripts", no "dependencies" — stub is minimal.

    index.js for each stub — a single throw statement:
    For @godoo-dev/client: throw new Error("@godoo-dev/client@0.0.0 is a placeholder for trusted publishing setup. Install the latest release: npm install @godoo-dev/client")
    For @godoo-dev/introspection: throw new Error("@godoo-dev/introspection@0.0.0 is a placeholder for trusted publishing setup. Install the latest release: npm install @godoo-dev/introspection")
    For @godoo-dev/testcontainers: throw new Error("@godoo-dev/testcontainers@0.0.0 is a placeholder for trusted publishing setup. Install the latest release: npm install @godoo-dev/testcontainers")

    Commit all six files to the release/stub-bootstrap branch with message:
    "chore(stubs): create throw-on-require 0.0.0 stub packages for @godoo-dev/* trusted publishing bootstrap"

    Do NOT push this branch to origin. Do NOT add _stubs/ to pnpm-workspace.yaml. Do NOT modify the root package.json.
  </action>
  <verify>
    <automated>
      node -e "require('./_stubs/@godoo-client-stub/index.js')" 2>&1 | grep -q "placeholder" && echo "client stub throws OK"
      node -e "require('./_stubs/@godoo-introspection-stub/index.js')" 2>&1 | grep -q "placeholder" && echo "introspection stub throws OK"
      node -e "require('./_stubs/@godoo-testcontainers-stub/index.js')" 2>&1 | grep -q "placeholder" && echo "testcontainers stub throws OK"
      jq -r '.name' _stubs/@godoo-client-stub/package.json | grep -q "@godoo-dev/client" && echo "name OK"
      jq -r '.version' _stubs/@godoo-client-stub/package.json | grep -q "0.0.0" && echo "version OK"
      jq -r '.publishConfig.access' _stubs/@godoo-client-stub/package.json | grep -q "public" && echo "publishConfig OK"
    </automated>
  </verify>
  <done>Six files exist in _stubs/; each index.js throws on require; each package.json has name, version 0.0.0, publishConfig.access public; branch is release/stub-bootstrap; no workspace modification.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Publish the three stubs to npm (requires OTP)</name>
  <read_first>
    - .planning/phases/03-publishing-source-repo-shedding/03-RESEARCH.md — Pattern 2 publish command, OTP note
    - .planning/phases/03-publishing-source-repo-shedding/03-CONTEXT.md — D-02 (stub publish from laptop); note: D-03 (break-glass token) is DROPPED — OIDC-only, no tokens
  </read_first>
  <what-built>
    Task 1 created three stub packages in _stubs/. They are ready to publish. Publishing requires npm 2FA OTP and cannot be automated.
  </what-built>
  <how-to-verify>
    Publish the three stubs one at a time (each will prompt for OTP or use --otp=CODE):

    1. cd _stubs/@godoo-dev-client-stub && npm publish --access public
    2. cd _stubs/@godoo-dev-introspection-stub && npm publish --access public
    3. cd _stubs/@godoo-dev-testcontainers-stub && npm publish --access public

    Confirm each appears on the registry:
    - npm view @godoo-dev/client version   (should return 0.0.0)
    - npm view @godoo-dev/introspection version   (should return 0.0.0)
    - npm view @godoo-dev/testcontainers version  (should return 0.0.0)
  </how-to-verify>
  <resume-signal>Type "stubs published" once all three npm view commands confirm 0.0.0.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local laptop → npm registry | Authenticated npm publish; OTP required; no CI involvement for stubs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | npm registry — stub package content | mitigate | Stub's index.js throws on require; `0.0.0` is below any `^x.y.z` semver constraint; no consumer can accidentally depend on it |
| T-03-02 | Spoofing | npm package name squatting (@godoo-dev/*) | mitigate | Publishing stubs immediately claims the names; the @godoo-dev org is already under Marc's control |
| T-03-03 | Information Disclosure | NPM_TOKEN_BREAKGLASS value | n/a | Break-glass token dropped 2026-05-26 — OIDC-only; no token exists. |
| T-03-04 | Elevation of Privilege | Stub pushed inside pnpm workspace | mitigate | _stubs/ directory is outside packages/; pnpm-workspace.yaml glob is 'packages/*'; changesets cannot discover stubs |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in workspace for Phase 3 — package legitimacy audit in RESEARCH.md confirms no workspace-level installs |
</threat_model>

<verification>
After Task 2 completes:
- npm view @godoo-dev/client version → 0.0.0
- npm view @godoo-dev/introspection version → 0.0.0
- npm view @godoo-dev/testcontainers version → 0.0.0
- The release/stub-bootstrap branch exists locally (git branch shows it)
- develop and main are unchanged (git diff develop..main shows no changes)
</verification>

<success_criteria>
Three @godoo-dev/* stub packages exist on npm at version 0.0.0 with throw-on-require mains. The npm scope @godoo-dev is claimed and ready for trusted publisher configuration. Release auth is OIDC-only — no npm tokens.
</success_criteria>

<output>
Create .planning/phases/03-publishing-source-repo-shedding/03-01-SUMMARY.md when done
</output>
