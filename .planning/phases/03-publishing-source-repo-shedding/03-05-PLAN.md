---
phase: 03-publishing-source-repo-shedding
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - scripts/deprecate-marcfargas.sh
autonomous: false
requirements:
  - SHED-01
  - SHED-02
  - SHED-03
  - SHED-04

must_haves:
  truths:
    - "All seven @marcfargas/odoo-* packages show deprecation warnings on npm install / npm view"
    - "scripts/deprecate-marcfargas.sh is committed with the exact D-04 messages verbatim"
    - "The deprecation messages for the three renamed packages point to @godoo/* and github.com/godoo-dev/godoo-ts"
    - "No new publish was made to any @marcfargas/* package — deprecation only, per D-04"
  artifacts:
    - path: "scripts/deprecate-marcfargas.sh"
      provides: "Audit-trail script for the seven npm deprecate calls"
      contains: "npm deprecate '@marcfargas/odoo-client@*'"
  key_links:
    - from: "scripts/deprecate-marcfargas.sh"
      to: "npm registry @marcfargas/* packages deprecation metadata"
      via: "npm deprecate with OTP"
      pattern: "npm view @marcfargas/odoo-client deprecated"
---

<objective>
Write and run the npm deprecation script for all seven retiring @marcfargas/odoo-* packages. Each package gets a precise per-package message (verbatim from D-04) pointing users to their correct successor.

Purpose: Completes the consumer-facing wind-down — anyone who tries to install a @marcfargas/odoo-* package now sees a deprecation warning with the correct redirect. No new code, no new publish, per the no-dual-maintenance principle.

Output: scripts/deprecate-marcfargas.sh committed; all seven @marcfargas/* packages deprecated on npm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-publishing-source-repo-shedding/03-CONTEXT.md
@.planning/phases/03-publishing-source-repo-shedding/03-RESEARCH.md
@.planning/phases/03-publishing-source-repo-shedding/03-PATTERNS.md
@.planning/phases/03-publishing-source-repo-shedding/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/deprecate-marcfargas.sh</name>
  <files>scripts/deprecate-marcfargas.sh</files>
  <read_first>
    - .planning/phases/03-publishing-source-repo-shedding/03-CONTEXT.md — D-04 block (AUTHORITATIVE source; exact messages are non-negotiable — copy verbatim)
    - .planning/phases/03-publishing-source-repo-shedding/03-PATTERNS.md — "scripts/deprecate-marcfargas.sh" section (exact shape)
    - .planning/phases/03-publishing-source-repo-shedding/03-RESEARCH.md — Pattern 5 (complete script), OTP note on 2FA, Claude's Discretion on committed helper
  </read_first>
  <action>
    Create scripts/deprecate-marcfargas.sh using the exact messages from CONTEXT.md D-04. No improvisation in wording — copy-paste the D-04 messages exactly.

    File header: #!/usr/bin/env bash with set -euo pipefail and a comment explaining the script is for one-shot execution locally with npm auth active.

    Seven npm deprecate calls, one per package, in this order (per D-04):
    1. @marcfargas/odoo-client@* — "Renamed to @godoo/client. Install that package instead — see https://github.com/godoo-dev/godoo-ts."
    2. @marcfargas/odoo-introspection@* — "Renamed to @godoo/introspection. Install that package instead — see https://github.com/godoo-dev/godoo-ts."
    3. @marcfargas/odoo-testcontainers@* — "Renamed to @godoo/testcontainers. Install that package instead — see https://github.com/godoo-dev/godoo-ts."
    4. @marcfargas/odoo-state-manager@* — "Superseded by godoo-stateman (Python). No JS shim — see https://github.com/godoo-dev for the godoo initiative."
    5. @marcfargas/odoo-cli@* — "Deprecated. No direct replacement — see https://github.com/godoo-dev for the godoo initiative."
    6. @marcfargas/odoo-mcp@* — "Deprecated. Source preserved for future salvage at https://github.com/marcfargas/odoo-toolbox (archived) targets/odoo-mcp/."
    7. @marcfargas/odoo-skills@* — "Deprecated. CI-generated knowledge modules — future home TBD in the Atlas KB."

    Add a comment above the OTP note: TOTP codes expire in ~30s; run interactively or pass --otp=CODE per call.

    Make the file executable: chmod +x scripts/deprecate-marcfargas.sh

    Commit to develop: "chore(shed): add deprecation script for seven retiring @marcfargas/odoo-* packages"
  </action>
  <verify>
    <automated>
      grep -c "npm deprecate" scripts/deprecate-marcfargas.sh | grep -q "^7$" && echo "7 deprecate calls OK"
      grep -q "odoo-client" scripts/deprecate-marcfargas.sh && echo "client OK"
      grep -q "odoo-introspection" scripts/deprecate-marcfargas.sh && echo "introspection OK"
      grep -q "odoo-testcontainers" scripts/deprecate-marcfargas.sh && echo "testcontainers OK"
      grep -q "odoo-state-manager" scripts/deprecate-marcfargas.sh && echo "state-manager OK"
      grep -q "odoo-cli" scripts/deprecate-marcfargas.sh && echo "cli OK"
      grep -q "odoo-mcp" scripts/deprecate-marcfargas.sh && echo "mcp OK"
      grep -q "odoo-skills" scripts/deprecate-marcfargas.sh && echo "skills OK"
      grep -q "set -euo pipefail" scripts/deprecate-marcfargas.sh && echo "safety header OK"
      grep -q "Renamed to @godoo/client" scripts/deprecate-marcfargas.sh && echo "exact message OK"
      grep -q "godoo-stateman" scripts/deprecate-marcfargas.sh && echo "state-manager message OK"
    </automated>
  </verify>
  <done>scripts/deprecate-marcfargas.sh exists with seven npm deprecate calls using verbatim D-04 messages; file has shebang + set -euo pipefail; committed to develop.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Execute the deprecation script (requires OTP per package)</name>
  <read_first>
    - scripts/deprecate-marcfargas.sh — the script to run
    - .planning/phases/03-publishing-source-repo-shedding/03-RESEARCH.md — Pattern 5 OTP note; 2FA means each call prompts or needs --otp=CODE
    - .planning/phases/03-publishing-source-repo-shedding/03-04-SUMMARY.md — confirm @godoo/* packages are live (deprecation messages direct users here)
  </read_first>
  <what-built>
    Task 1 created scripts/deprecate-marcfargas.sh with the exact D-04 messages for all seven retiring @marcfargas/* packages. Plan 03-04 confirmed @godoo/* packages are live on npm so deprecation redirects are valid.
  </what-built>
  <how-to-verify>
    The script must be run locally (not in CI — npm deprecate requires auth + OTP and is a one-shot operation).

    Run each deprecate call individually (OTP expires in ~30s, cannot batch all seven with one OTP):
    Option A — run one at a time with explicit OTP:
    npm deprecate '@marcfargas/odoo-client@*' 'Renamed to @godoo/client. Install that package instead — see https://github.com/godoo-dev/godoo-ts.' --otp=<your-current-totp-code>
    (repeat for each of the seven packages with a fresh OTP code each time)

    Option B — run the script and respond to each interactive OTP prompt:
    ./scripts/deprecate-marcfargas.sh
    (set -euo pipefail will stop on first failure; if an OTP is invalid the script stops and you resume from that package)

    After all seven calls complete, verify each package shows the deprecation message:
    npm view @marcfargas/odoo-client deprecated
    npm view @marcfargas/odoo-introspection deprecated
    npm view @marcfargas/odoo-testcontainers deprecated
    npm view @marcfargas/odoo-state-manager deprecated
    npm view @marcfargas/odoo-cli deprecated
    npm view @marcfargas/odoo-mcp deprecated
    npm view @marcfargas/odoo-skills deprecated
    (Each should print the exact D-04 message for that package)
  </how-to-verify>
  <resume-signal>Type "deprecated" once all seven npm view commands print their deprecation messages.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local laptop → npm registry (deprecate) | Authenticated npm CLI with OTP; modifies existing package metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | Spoofing | Incorrect deprecation message misleads users | mitigate | Messages are copy-paste verbatim from CONTEXT.md D-04; no improvisation; grep gate in Task 1 verify checks exact message text |
| T-03-19 | Tampering | Deprecation messages applied to wrong package versions (not @*) | mitigate | All seven calls use @* to deprecate all versions; no final release precedes deprecation |
| T-03-20 | Information Disclosure | TOTP code captured in shell history | accept | TOTP codes expire in 30s; historical exposure is time-bounded; if using --otp flag, value is in history but useless after expiry |
| T-03-21 | Tampering | New publish made to @marcfargas/* after deprecation | mitigate | The no-dual-maintenance principle and D-04 explicitly forbid any final release; scripts/deprecate-marcfargas.sh is committed as audit trail |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new workspace packages installed |
</threat_model>

<verification>
After both tasks:
- npm view @marcfargas/odoo-client deprecated → "Renamed to @godoo/client. Install that package instead — see https://github.com/godoo-dev/godoo-ts."
- npm view @marcfargas/odoo-introspection deprecated → "Renamed to @godoo/introspection. Install that package instead — see https://github.com/godoo-dev/godoo-ts."
- npm view @marcfargas/odoo-testcontainers deprecated → "Renamed to @godoo/testcontainers. Install that package instead — see https://github.com/godoo-dev/godoo-ts."
- npm view @marcfargas/odoo-state-manager deprecated → "Superseded by godoo-stateman (Python). No JS shim — see https://github.com/godoo-dev for the godoo initiative."
- npm view @marcfargas/odoo-cli deprecated → "Deprecated. No direct replacement — see https://github.com/godoo-dev for the godoo initiative."
- npm view @marcfargas/odoo-mcp deprecated → "Deprecated. Source preserved for future salvage at https://github.com/marcfargas/odoo-toolbox (archived) targets/odoo-mcp/."
- npm view @marcfargas/odoo-skills deprecated → "Deprecated. CI-generated knowledge modules — future home TBD in the Atlas KB."
- scripts/deprecate-marcfargas.sh committed on develop with 7 deprecate calls
</verification>

<success_criteria>
All seven @marcfargas/odoo-* packages display their exact D-04 deprecation messages on the npm registry. SHED-01 (odoo-skills ejected/deprecated), SHED-02 (odoo-cli deprecated), SHED-03 (odoo-mcp deprecated), and SHED-04 (odoo-state-manager deprecated) are complete.
</success_criteria>

<output>
Create .planning/phases/03-publishing-source-repo-shedding/03-05-SUMMARY.md when done
</output>
