---
phase: 03-publishing-source-repo-shedding
plan: 06
type: execute
wave: 5
depends_on:
  - 03-04
files_modified: []
autonomous: false
requirements:
  - SHED-05

must_haves:
  truths:
    - "PR1 merges to master on odoo-toolbox removing packages/odoo-client/, packages/odoo-introspection/, packages/odoo-testcontainers/"
    - "PR2 merges to master on odoo-toolbox removing non-adopted leftovers, preserving targets/odoo-mcp/, and replacing README.md with a deprecation pointer (D-06)"
    - "The odoo-toolbox master branch is renamed to main"
    - "odoo-toolbox is GitHub-archived (read-only)"
    - "targets/odoo-mcp/ is preserved in the archived tree for future Atlas MCP charter"
  artifacts: []
  key_links:
    - from: "odoo-toolbox godoo-adoption branch PR1"
      to: "packages/odoo-client, packages/odoo-introspection, packages/odoo-testcontainers removed"
      via: "three commits (one per package for bisectability per D-05)"
      pattern: "git log --oneline godoo-adoption | grep shed"
    - from: "odoo-toolbox godoo-adoption branch PR2"
      to: "retirement README.md + preserved targets/odoo-mcp/"
      via: "single commit removing non-adopted packages + rewriting README"
      pattern: "ls C:/dev/odoo-toolbox/targets/odoo-mcp/"
---

<objective>
Execute the source-side shed on odoo-toolbox under the godoo-adoption protocol: two PRs on a godoo-adoption branch that remove all adopted and non-adopted packages, write the deprecation README, rename master to main, and archive the repo.

This is the destination-side's contractual obligation under the godoo-adoption protocol — the three @godoo/* packages are now live (Plan 03-04), so the source packages must be removed from odoo-toolbox.

Purpose: Completes the no-dual-maintenance guarantee. After this plan, each package lives in exactly one repo. The pre-adoption-baseline tag and the targets/odoo-mcp/ salvage tree remain queryable in the frozen archive.

Output: odoo-toolbox is read-only, with a deprecation README on main and targets/odoo-mcp/ preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-publishing-source-repo-shedding/03-CONTEXT.md
@.planning/phases/03-publishing-source-repo-shedding/03-RESEARCH.md
@.planning/phases/03-publishing-source-repo-shedding/03-04-SUMMARY.md
@../godoo-hq/.planning/notes/godoo-adoption-protocol.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PR1 — Remove adopted core packages from odoo-toolbox</name>
  <files>
    C:/dev/odoo-toolbox/packages/odoo-client/ (removed)
    C:/dev/odoo-toolbox/packages/odoo-introspection/ (removed)
    C:/dev/odoo-toolbox/packages/odoo-testcontainers/ (removed)
    C:/dev/odoo-toolbox/package.json (workspaces updated)
  </files>
  <read_first>
    - ../godoo-hq/.planning/notes/godoo-adoption-protocol.md — source-side step 2: remove each package as it is adopted; one commit per package for bisectability
    - .planning/phases/03-publishing-source-repo-shedding/03-CONTEXT.md — D-05 (two-PR shed, one commit per package for bisectability, PR1 scope), D-06 (PR2 scope)
    - .planning/phases/03-publishing-source-repo-shedding/03-RESEARCH.md — Code Examples "odoo-toolbox PR1 commit sequence", open question A5 (branch protection on odoo-toolbox master), A6 (godoo-adoption branch state)
    - .planning/phases/03-publishing-source-repo-shedding/03-04-SUMMARY.md — confirm @godoo/* packages are live before removing source copies
  </read_first>
  <action>
    Work in C:/dev/odoo-toolbox (NOT in godoo-ts).

    First, check whether a godoo-adoption branch already exists on odoo-toolbox:
    git -C C:/dev/odoo-toolbox branch -a | grep godoo-adoption

    If it does not exist: create it from master:
    git -C C:/dev/odoo-toolbox checkout master && git -C C:/dev/odoo-toolbox pull origin master
    git -C C:/dev/odoo-toolbox checkout -b godoo-adoption

    If it exists and appears stale: inspect its state and rebase/reset as needed before proceeding.

    Also check whether odoo-toolbox has a main branch that may conflict with the later rename (Open Question 4 in RESEARCH.md):
    git -C C:/dev/odoo-toolbox branch -a | grep main
    If a main branch exists, note this for Task 3 (it may need force-deletion before rename).

    Three commits on the godoo-adoption branch — one per package (D-05: one commit per package for bisectability against pre-adoption-baseline):

    Commit 1 — remove packages/odoo-client/:
    git -C C:/dev/odoo-toolbox rm -r packages/odoo-client/
    Edit packages/odoo-toolbox/package.json workspaces array to remove "packages/odoo-client" entry; also remove from any build script chain that references it.
    git -C C:/dev/odoo-toolbox add package.json
    git -C C:/dev/odoo-toolbox commit -m "shed(client): remove packages/odoo-client/ — adopted as @godoo/client in godoo-ts"

    Commit 2 — remove packages/odoo-introspection/:
    git -C C:/dev/odoo-toolbox rm -r packages/odoo-introspection/
    Edit package.json to remove odoo-introspection workspace entry.
    git -C C:/dev/odoo-toolbox add package.json
    git -C C:/dev/odoo-toolbox commit -m "shed(introspection): remove packages/odoo-introspection/ — adopted as @godoo/introspection in godoo-ts"

    Commit 3 — remove packages/odoo-testcontainers/:
    git -C C:/dev/odoo-toolbox rm -r packages/odoo-testcontainers/
    Edit package.json to remove odoo-testcontainers workspace entry.
    git -C C:/dev/odoo-toolbox add package.json
    git -C C:/dev/odoo-toolbox commit -m "shed(testcontainers): remove packages/odoo-testcontainers/ — adopted as @godoo/testcontainers in godoo-ts"

    Push godoo-adoption to origin:
    git -C C:/dev/odoo-toolbox push origin godoo-adoption

    Open PR1 on odoo-toolbox (base: master, head: godoo-adoption):
    gh pr create --repo marcfargas/odoo-toolbox \
      --base master \
      --head godoo-adoption \
      --title "shed(core-3): remove adopted packages — @godoo/* now live in godoo-ts" \
      --body "Removes packages/odoo-client/, packages/odoo-introspection/, packages/odoo-testcontainers/ per the godoo-adoption protocol. Each package is now live at @godoo/* in godoo-dev/godoo-ts. Three commits — one per package — for bisectability against the pre-adoption-baseline tag."

    Check if branch protection requires reviews on master; if so, approve and merge from the UI:
    gh pr checks --repo marcfargas/odoo-toolbox
    gh pr merge --repo marcfargas/odoo-toolbox <pr-number> --squash

    Note: do NOT delete the godoo-adoption branch after PR1 — PR2 will be opened from the same branch.
  </action>
  <verify>
    <automated>
      test ! -d C:/dev/odoo-toolbox/packages/odoo-client && echo "odoo-client removed"
      test ! -d C:/dev/odoo-toolbox/packages/odoo-introspection && echo "odoo-introspection removed"
      test ! -d C:/dev/odoo-toolbox/packages/odoo-testcontainers && echo "odoo-testcontainers removed"
      git -C C:/dev/odoo-toolbox log --oneline godoo-adoption | grep -c "shed(" | grep "3" && echo "3 shed commits on branch"
    </automated>
  </verify>
  <done>PR1 merged to master on odoo-toolbox; packages/odoo-client/, packages/odoo-introspection/, packages/odoo-testcontainers/ are gone from master; three shed commits visible on godoo-adoption branch history.</done>
</task>

<task type="auto">
  <name>Task 2: PR2 — Wind down non-adopted packages and write retirement README</name>
  <files>
    C:/dev/odoo-toolbox/packages/odoo-state-manager/ (removed)
    C:/dev/odoo-toolbox/packages/odoo-skills/ (removed)
    C:/dev/odoo-toolbox/packages/odoo-test-harness/ (removed)
    C:/dev/odoo-toolbox/targets/odoo-cli/ (removed)
    C:/dev/odoo-toolbox/targets/odoo-mcp/ (PRESERVED — do not touch)
    C:/dev/odoo-toolbox/README.md (replaced with deprecation pointer)
    C:/dev/odoo-toolbox/package.json (trimmed)
    C:/dev/odoo-toolbox/tsconfig.json (removed)
    C:/dev/odoo-toolbox/docker-compose.test.yml (removed)
  </files>
  <read_first>
    - .planning/phases/03-publishing-source-repo-shedding/03-CONTEXT.md — D-06 (AUTHORITATIVE: PR2 exact contents, preserved paths, retirement README required sections, directories to remove)
    - ../godoo-hq/UMBRELLA_CLAUDE.md — no-copies and siblings-never-nested rules inform the retirement README framing
    - .planning/phases/03-publishing-source-repo-shedding/03-CONTEXT.md — D-04 deprecation messages (the README links godoo-ts, the @godoo/* package names, and mentions the pre-adoption-baseline tag)
  </read_first>
  <action>
    Continue on the godoo-adoption branch in C:/dev/odoo-toolbox (PR1 already merged; godoo-adoption now diverges from the updated master by this PR2 work).

    First, pull master and rebase godoo-adoption on top of the PR1-merged master:
    git -C C:/dev/odoo-toolbox fetch origin
    git -C C:/dev/odoo-toolbox checkout godoo-adoption
    git -C C:/dev/odoo-toolbox rebase origin/master

    Remove the non-adopted package directories (per D-06):
    - packages/odoo-state-manager/
    - packages/odoo-skills/
    - packages/odoo-test-harness/
    - targets/odoo-cli/
    DO NOT remove targets/odoo-mcp/ — it is explicitly preserved as the Atlas MCP salvage source per D-06.

    Remove root-level tooling files that belong to the retired project (per D-06):
    - tsconfig.json
    - vitest.config.mts, vitest.integration.config.mts, vitest.packaging.config.mts
    - docker-compose.test.yml
    - docker/ directory
    - scripts/ directory
    - tests/ directory
    - dist-plugins/ and dist-skills/ directories (if they exist)
    - targets/ directory MINUS targets/odoo-mcp/ (remove all targets/* except odoo-mcp)
    - docs/ directory (if exists)
    - skills/ directory (if exists)
    - experiments/ directory (if exists)
    - test-addons/ directory (if exists)
    - package-lock.json (lockfile, no longer needed)
    Trim root package.json to remove workspaces array entries for removed packages (odoo-state-manager, odoo-skills, odoo-test-harness, odoo-cli) and strip scripts/devDependencies to empty or minimal.

    Write the retirement README.md (D-06 authoritative sections, in this order):
    1. Heading: "odoo-toolbox — Archived"
    2. Package redirect table with new npm names and godoo-ts link:
       - @marcfargas/odoo-client → @godoo/client (npm link + godoo-ts GitHub link)
       - @marcfargas/odoo-introspection → @godoo/introspection
       - @marcfargas/odoo-testcontainers → @godoo/testcontainers
    3. Link to https://github.com/godoo-dev/godoo-ts for ongoing development
    4. Note: odoo-state-manager is superseded by godoo-stateman (Python, no JS shim, own timeline)
    5. Note: targets/odoo-mcp/ is preserved in this archive as the canonical salvage source for a future Atlas MCP charter, with a relative link to ./targets/odoo-mcp/ so archived-repo viewers can navigate in two clicks
    6. Historical reference: the pre-adoption-baseline git tag marks the last state before adoption (link: https://github.com/marcfargas/odoo-toolbox/tree/pre-adoption-baseline)
    7. LGPL-3.0 license notice (carried forward per D-06)

    Commit all PR2 changes as a single commit:
    git -C C:/dev/odoo-toolbox add -A
    git -C C:/dev/odoo-toolbox commit -m "shed(all): wind down non-adopted packages, write retirement README — odoo-toolbox retired"

    Push and open PR2:
    git -C C:/dev/odoo-toolbox push origin godoo-adoption --force-with-lease
    gh pr create --repo marcfargas/odoo-toolbox \
      --base master \
      --head godoo-adoption \
      --title "shed(wind-down): retire odoo-toolbox — deprecation README + non-adopted package removal" \
      --body "Removes packages/odoo-state-manager/, packages/odoo-skills/, packages/odoo-test-harness/, targets/odoo-cli/. Preserves targets/odoo-mcp/ as Atlas MCP salvage source. Replaces README.md with deprecation pointer to @godoo/* and godoo-dev/godoo-ts. Trims root toolchain files. Completes the godoo-adoption protocol source-side shed."

    Merge PR2:
    gh pr merge --repo marcfargas/odoo-toolbox <pr-number> --squash
  </action>
  <verify>
    <automated>
      test ! -d C:/dev/odoo-toolbox/packages/odoo-state-manager && echo "state-manager removed"
      test ! -d C:/dev/odoo-toolbox/packages/odoo-skills && echo "skills removed"
      test ! -d C:/dev/odoo-toolbox/targets/odoo-cli && echo "odoo-cli removed"
      test -d C:/dev/odoo-toolbox/targets/odoo-mcp && echo "odoo-mcp PRESERVED"
      grep -q "@godoo/client" C:/dev/odoo-toolbox/README.md && echo "README has @godoo/client"
      grep -q "pre-adoption-baseline" C:/dev/odoo-toolbox/README.md && echo "README has baseline tag"
      grep -q "targets/odoo-mcp" C:/dev/odoo-toolbox/README.md && echo "README links odoo-mcp"
      grep -q "LGPL" C:/dev/odoo-toolbox/README.md && echo "README has LGPL notice"
    </automated>
  </verify>
  <done>PR2 merged to master; non-adopted package directories removed; targets/odoo-mcp/ preserved; root README.md is the deprecation pointer with all D-06 required sections; LGPL notice present.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Rename master to main and archive odoo-toolbox</name>
  <read_first>
    - .planning/phases/03-publishing-source-repo-shedding/03-CONTEXT.md — D-06 final sentences: "After PR2 merges, rename master → main (matching the project's main is release, develop is working convention) and then GitHub-archive the repo (read-only)"
    - .planning/phases/03-publishing-source-repo-shedding/03-RESEARCH.md — Pitfall 5 (rename AFTER PR2 merge, BEFORE archive), Pitfall 6 (archive PREVENTS rename — strict sequence), Don't Hand-Roll (branch rename command, archive command)
    - .planning/phases/03-publishing-source-repo-shedding/03-RESEARCH.md — Open Question 4 (odoo-toolbox already has both master and main branches)
  </read_first>
  <what-built>
    Tasks 1 and 2 merged PR1 and PR2 to master on odoo-toolbox. The repo is now in its retired state on master. Now rename master → main and archive the repo.

    CRITICAL SEQUENCE: rename BEFORE archive (archiving locks the repo; rename after archive is impossible).
  </what-built>
  <how-to-verify>
    First, check if a main branch already exists on odoo-toolbox (Open Question 4):
    gh api repos/marcfargas/odoo-toolbox/branches/main 2>/dev/null && echo "main exists" || echo "main does not exist"

    If a main branch exists AND it has diverged from master:
    - Force-delete the stale main branch BEFORE renaming:
      gh api --method DELETE repos/marcfargas/odoo-toolbox/git/refs/heads/main
    - Then proceed with rename below.

    Step 1: Rename master → main (strictly after PR2 merge, strictly before archive):
    gh api --method POST repos/marcfargas/odoo-toolbox/branches/master/rename \
      --field new_name=main

    Step 2: Update the default branch to main:
    gh api --method PATCH repos/marcfargas/odoo-toolbox \
      --field default_branch=main

    Step 3: Archive the repo (read-only):
    gh repo archive marcfargas/odoo-toolbox --yes

    Verify:
    - gh api repos/marcfargas/odoo-toolbox | jq -r '.default_branch'  → "main"
    - gh api repos/marcfargas/odoo-toolbox | jq -r '.archived'  → true
    - gh api repos/marcfargas/odoo-toolbox/branches/main → returns the branch (not 404)
    - gh api repos/marcfargas/odoo-toolbox/branches/master → returns 404 (branch renamed)
    - Navigate to https://github.com/marcfargas/odoo-toolbox to visually confirm the "Archived" badge and the deprecation README on main
  </how-to-verify>
  <resume-signal>Type "archived" once gh api confirms archived: true and default_branch: main, and the GitHub page shows the archived badge.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git + gh CLI → odoo-toolbox GitHub repo | Destructive operations (rm, branch rename, archive) on the source repo |
| godoo-adoption branch → master on odoo-toolbox | PR merge changes the canonical source; no review gate unless branch protection requires it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-22 | Tampering | targets/odoo-mcp/ accidentally deleted in PR2 | mitigate | Task 2 explicitly skips odoo-mcp during rm -r; verify step checks test -d targets/odoo-mcp |
| T-03-23 | Tampering | Archive before rename (Pitfall 6) | mitigate | Task 3 human checkpoint explicitly orders rename (step 1) before archive (step 3); the Pitfall 6 warning is restated in the task |
| T-03-24 | Repudiation | pre-adoption-baseline tag lost during shed | mitigate | Tags are not removed by git rm; the tag is safe unless explicitly deleted; retirement README links it for discoverability |
| T-03-25 | Tampering | godoo-adoption branch continues to exist after PR2 (confusing state) | accept | The archived repo locks all branches; stale godoo-adoption branch is harmless in read-only repo |
| T-03-26 | Denial of Service | Existing main branch on odoo-toolbox conflicts with rename | mitigate | Task 3 explicitly checks for existing main; if diverged, force-delete before rename (with human confirmation) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan |
</threat_model>

<verification>
Final state after all three tasks:
- gh api repos/marcfargas/odoo-toolbox | jq '{archived:.archived, default_branch:.default_branch}' → {"archived":true,"default_branch":"main"}
- gh api repos/marcfargas/odoo-toolbox/branches/master → 404 (renamed)
- gh api repos/marcfargas/odoo-toolbox/branches/main → 200
- ls C:/dev/odoo-toolbox/targets/odoo-mcp/ → contents present (preserved)
- cat C:/dev/odoo-toolbox/README.md | grep -q "@godoo/client" → match
- cat C:/dev/odoo-toolbox/README.md | grep -q "pre-adoption-baseline" → match
- cat C:/dev/odoo-toolbox/README.md | grep -q "targets/odoo-mcp" → match
- test ! -d C:/dev/odoo-toolbox/packages/odoo-client → directory absent
</verification>

<success_criteria>
odoo-toolbox is archived on GitHub (read-only) with main as the default branch. The deprecation README is live on main with links to @godoo/*, godoo-dev/godoo-ts, the pre-adoption-baseline tag, and the targets/odoo-mcp/ salvage tree. All adopted and non-adopted packages are removed from the repo. targets/odoo-mcp/ is preserved. SHED-05 is complete.
</success_criteria>

<output>
Create .planning/phases/03-publishing-source-repo-shedding/03-06-SUMMARY.md when done
</output>
